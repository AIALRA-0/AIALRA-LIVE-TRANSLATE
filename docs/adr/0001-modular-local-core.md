# ADR-0001 本地模块化核心

## 1 状态

状态记录日期来自本次实施日志：2026-08-24，已接受

## 2 决定

Rust 模块化单体负责会话状态、事件、音频 ACK、对象存储、SQLite、策略和本地 HTTP 接口

模型工作放在独立 Python 进程

## 3 理由

音频可靠性需要可预测的内存与并发边界，模型生态需要 Python

进程分离让模型失败保持降级状态，录音和停止控制仍可工作

## 4 后果

首版部署需要两个本地进程

协议升级必须版本化，未来可以在不改核心存储的情况下替换模型
