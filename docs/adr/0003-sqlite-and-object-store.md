# ADR-0003 SQLite WAL 内容寻址对象

## 1 状态

状态记录日期来自本次实施日志：2026-08-24，已接受

## 2 决定

单用户默认存储使用 SQLite WAL

记录于事件协议源代码的存储规则要求音频和材料以 SHA-256 内容地址存放，数据库只保存哈希与元数据

## 3 理由

该组合无需运维数据库，支持事务、幂等和断电恢复

内容地址可以去重并阻止用户文件名成为路径

## 4 后果

多用户远程服务需要重新评估数据库

删除对象前必须检查引用，不能只删数据库记录
