# 变更记录

## 2026-09-05 v29 最小收尾上线

- 直接在 `main` 修复部署器回滚环境变量污染：回滚 v28 时显式清除候选 `AIALRA_BUILD_ID`，避免旧镜像继承新版本 build id。
- 从运行代码 SHA `5e72c836ca8f189122e3f5436e9eacf9fda1ea45` 远端构建并部署 `quality-v29-20260905-5e72c83`；发布目录 `BUILD_ID`、Core `build_id` 和 OCI revision 三方一致。
- 健康门通过：Core healthy、restart count `0`、GPU Worker 在线、ASR/LLM provider 为 CUDA、模型队列 `queued=0`/`leased=0`；v28 回滚资源保留。
- 生产快速冒烟通过：同项目第二设备 `409`、双项目并行租约、21/21 durable ACK 含 `commit_id`、字幕、译文、材料自动讲解、ReadWeave 读回、安全停止、重复停止和未认证边界检查均通过。
- 本次未执行 6 小时/24 小时门禁、Android 真机矩阵、MacBook 物理麦克风确认或 Windows 崩溃调查；这些不能由本次快速上线结果替代。

## 2026-09-05 历史部署记录：v29 首次候选回滚

- 远端构建了 `quality-v29-20260905-501143a` 候选镜像；Web 构建与 Rust Core 编译通过，但切换后的健康请求超时，部署器自动回滚，未进入 v29 生产冒烟。
- 回滚后的线上版本为 `quality-v28-20260901-1849013`，Core healthy、restart count 为 0，GPU Worker 在线且 ASR／LLM provider 为 CUDA，模型队列排空。
- v28 最小复核确认租约、同项目第二设备 `409`、带 `commit_id` 的 durable ACK、安全停止和归档；v29 材料自动讲解行为尚未得到生产证明。
- 回滚子进程继承候选 `AIALRA_BUILD_ID` 的问题已在运行环境中通过清除继承变量并按 v28 `.env` 重建 Core 收口；未修改代码、数据库或其他项目资源。
- 当前发布判定：`V28 ONLINE / V29 DEPLOYMENT BLOCKED`。下一步只处理 v29 启动/健康超时的最小根因，不扩张功能。

## feedback-11 2026-09-01 录音状态、工作区与材料讲解（已合并，部署待容量）

- 录音状态区分租约冲突、GPU 容量不足、可恢复历史、处理中和不可继续录音；用户确认后可沿用未完成会话追加，保留时间戳，不自动清理过期租约。
- 录音状态事件会即时刷新工作区指示灯；不同项目可并行录音，容量门只拒绝新项目，不影响已有录音续租、ACK 和停止。
- 新建文件夹／项目改由根目录或文件夹右键菜单提供；拖拽使用独立手柄和明确的前面／里面／后面落点，移动失败不乐观改写树；回收站阻断显示可读原因。
- 移除“课程笔记分类”标题，补齐侧栏主题、设置状态窗口和材料区域；材料上传移到时间线底部，确认后才创建解析任务和等待解析完成的幂等讲解任务，连续上传合并，解析失败不执行讲解。
- Rust workspace 63 项测试、Clippy、Web 27 项测试、类型检查、ESLint、生产构建和 `git diff --check` 通过；本地 Docker／Docker Desktop／WSL 未使用。
- PR #15 已完成自审并 squash 合并为 `44a8dbfc036bfc769d8edc9fcaa3c23d0cfba3d1`；远程 v29 尚未部署。
- 部署前只读检查显示 VPS 根分区约 18.0 GiB 可用、可用比例约 4%，未满足部署器的容量门槛；未构建、未切换、未重启 GPU Agent，也未清理共享 Docker 资源。真实浏览器和生产冒烟待容量恢复后进行。

## feedback-11 2026-09-01 部署器回滚与环境读取

- 部署器会从既有 `.env` 读取 `AIALRA_DATA_PATH` 和 `AIALRA_AUTH_SERVICE`，命令行显式环境变量仍优先。
- 构建前为旧容器镜像保留按旧 `BUILD_ID` 命名的不可变标签；回滚时强制重建、禁止隐式构建，并核对恢复容器的 image ID。
- 修复来自 v28 首次部署的真实失败路径；当前线上 v28 已使用正确显式参数完成部署，本项不改变运行时业务代码。

## feedback-10 2026-09-01 窄屏运行状态窗口（已部署）

- 390 像素真实 Chromium 验证发现运行状态全屏窗口与遮罩内边距叠加，造成横向越界；移动端专用遮罩现取消内边距并拉伸到视口。
- PR #13 已 squash 合并为 `1849013ce2e04dd8f098df6652b4e57462c0974c` 并远程部署 `quality-v28-20260901-1849013`；Web 26 项、类型检查、ESLint、生产构建及部署后桌面／390 像素 Chromium 通过。

## feedback-09 2026-09-01 多项目录音、语言与工作区交互（已部署）

- 新增项目录音状态接口，返回当前标签页是否持有租约、租约到期时间和新录音容量状态，不返回租约令牌或设备标识。
- 同一项目保持单录音租约；不同标签页改用独立录音身份，因此可分别录制不同项目。工作区偏好继续使用稳定的浏览器设备身份。
- 新录音增加 ASR Worker、CUDA Provider 和 ASR 积压容量门；已有录音的恢复、续租、ACK 与停止绕过该门。
- 项目支持修改后续课程的默认讲授／翻译语言，新建课程可单独选择；接口保持可选字段兼容。
- 新增原子工作区移动接口，拖拽显示前置插入线、文件夹容器高亮和后置插入线，失败时不预先改写树。
- 录音冲突通过项目事件和低频状态复查收敛，释放或到期后自动恢复；会话状态变化会刷新侧栏指示灯。
- 侧栏完整跟随亮暗主题，移除“课程笔记分类”标题，重做设置与运行状态面板，并保留麦克风具体名称、重新枚举和按项目选择。
- PR #12 已 squash 合并为 `c69384cb4e19bd38fa5e17d8862052bb36410c89`，并远程部署 `quality-v27-20260901-c69384c`；SHA 追溯、Core healthy 和 restart count 0 均通过。
- 生产合成链路与双项目并发租约短测通过；桌面真实 Chromium 的语言、主题和设置窗口通过，390 像素设置窗口越界由 feedback-10 候选收口。
- 本批未使用本地 Docker、Docker Desktop 或 WSL。

## feedback-07／feedback-08 2026-09-01 录音续接、麦克风设备与拖拽反馈（已部署）

- 工作区拖拽增加合法目标判断、目标高亮、放置提示和当前移动对象提示，非法拖放不会提交移动。
- 项目概览优先提供未完成会话的“继续本次收音”入口；同一会话保留已确认历史，显示建立时间和最近活动时间，已完成会话不重新覆盖。
- 麦克风授权后重新枚举并显示具体设备名称，保存设备选择、监听设备插拔，并将选择用于下一次收音与本地测试，修复 macOS 授权前标签不完整导致的切换困惑。
- 新增 Web UI 状态与设备标签回归测试；PR #9 已 squash 合并，PR #10 修复设备列表刷新失败不应中断已准备好的录音，随后以 `main` SHA `e2acd0190b72f173318afc223cfa231e4933ccda` 部署。
- `quality-v26-20260901-e2acd0` 的发布目录、Core `build_id` 和 OCI revision 一致；Core healthy、restart count 为 0，模型队列排空。
- 生产合成冒烟通过：21／21 durable ACK 含 `commit_id`、第二设备 `409`、6 条稳定字幕、2 条稳定译文、1 页材料、1 张讲解卡、会话完成和 ReadWeave 2 条结果可读。
- 本批未使用本地 Docker／Docker Desktop／WSL；真实 macOS 麦克风型号与切换仍待用户在已登录浏览器中确认。

## feedback-03／feedback-04 2026-09-01 ReadWeave 持久化修复与生产读回（已完成）

- PR #5 修复 ReadWeave 在 `PUT` 后清理 HTML 注释造成的管理标记丢失：新投影使用非可见 `data-*` 标记，旧注释标记仍可读；只有生成正文未被改动时才自动恢复，人工修改继续安全冲突
- 运行代码已 squash 合并到 `2c43fb82cf672538b5104bbf9211d4a67480eede` 并部署；随后 PR #6／#7 只追加脱敏状态文档，远端短期分支已删除；本机未运行 Docker／Docker Desktop／WSL
- 远端部署版本为 `quality-v25-20260901-2c43fb8`；发布目录 `BUILD_ID`、Core `build_id` 和 OCI revision 一致，Core／ReadWeave healthy，Core restart count 为 0
- 重启唯一 GPU stack 后，Worker 健康检查确认 ASR 和 Ollama 可用；生产合成冒烟通过：21／21 durable ACK 含 `commit_id`、第二设备 `409`、字幕／译文／材料／讲解生成、安全停止、会话完成和 ReadWeave 读回均通过
- 冒烟结束后活动租约与活动模型任务为 0；新投影的 7 个 ReadWeave 映射均保留管理和匿名对象标记。上一轮失败冒烟的 7 条冲突记录仅作为历史证据保留
- 6 小时、24 小时、真机矩阵和 Windows 崩溃调查没有在本轮执行；真实 Authentik 登录后的物理麦克风体验留给用户实际反馈

## feedback-01 2026-09-01 后台错误路径、录音认证与工作区收口（历史候选记录）

本节中的“待远端发布”表述是提交当时的状态，已由上面的 PR #5 合并、远端部署和生产读回覆盖。

- 修复浏览器录音控制请求误落入 Android 免认证代理规则的问题；增加独立设备控制别名，保持浏览器 URL、方法、登录方式和 Android 协议兼容。
- durable 音频写入后的项目通知／ReadWeave 辅助失败不再否定 ACK；停止流程幂等化，GPU Agent 领取后的已知异常统一安全上报，网关完成回写失败单独归类。
- 增加 ReadWeave 映射恢复、重复校对幂等、所有权校验和安全删除失败关闭策略；日志不输出原始异常、载荷、模型正文、转写、音频、令牌或私有路径。
- 增加回收站、右键工作区操作、展开／折叠、拖拽排序／移动、分类系统笔记、完整主题和只读设置／运行状态；ReadWeave 连接目标、范围、最近同步和打开前确认明确展示。
- 本地候选未使用 Docker／WSL；Rust、Python、Web 快速检查通过，Android 当前受本机 Java loopback 环境错误影响，远端 Bash、合并、部署和新生产冒烟尚未完成。

## 0.5.2 2026-09-01 v25 基础快速上线（已由 2c43fb8 重部署）

- 模型任务失败请求新增向后兼容的 `error_stage` 和 `diagnostic_id`；GPU Agent 将失败分为网关响应、任务载荷、模型 HTTP、模型 JSON 与执行设备，并在失败报告重试时复用诊断标识
- 隐私安全日志只保留诊断标识、任务类型、阶段、错误类别、HTTP 状态、响应长度与摘要，不写入模型正文、转写、音频、令牌、真实会话 ID或完整载荷
- Core 健康响应新增 `build_id`；发布目录 `BUILD_ID`、生产 Compose 和 OCI revision 绑定同一完整 `main` SHA
- 部署器在构建后、切换前检查磁盘、活动录音租约和模型队列，短暂停 Core 后备份 SQLite/WAL/SHM；失败只回滚代码、容器与配置，不自动覆盖持久数据
- 生产冒烟工具支持本地 SSH 隧道控制 API 与公网 WSS 音频入口，并验证第二设备 `409`、durable ACK `commit_id`、字幕、译文、讲解、停止、会话完成、ReadWeave 读回和队列排空
- 候选已通过 Rust workspace 48 项、Clippy、Python 33 项、Ruff、mypy、Web 21 项、类型检查、ESLint、生产构建、Node 语法、VPS Bash `-n` 和 `git diff --check`
- `quality-v25-20260901-43a61ad` 已从已发布源码提交部署；`BUILD_ID`、Core `build_id` 和 OCI revision 一致，Core healthy、重启计数 0
- 生产合成音频冒烟通过：第二设备 `409`、21／21 个 durable ACK 均含 `commit_id`、6 条稳定字幕、2 条稳定译文、1 页材料抽取、1 张讲解卡、安全停止、会话完成、ReadWeave 可读且队列排空

## 0.5.1 2026-09-01 公开候选自审加固

- 最新脱敏候选已通过完整 diff 自审和隔离签名认证，未决发现为 0；GitHub PR #2 已 squash 合并，合并后 `main` 文件树与认证候选精确一致，短期分支已删除
- 旧 PR #1 已注明被最新候选取代并关闭，远端旧分支已删除；本地常态收敛为一个工作目录和一个跟踪 `origin/main` 的长期 `main`
- Gradle 启动脚本改为每次执行都复核缓存 Wrapper JAR 的固定 SHA-256；缓存缺失或不匹配时从已审计 Base64 文本原子恢复，并原子刷新匹配的 Wrapper properties
- IDOR WebSocket 门禁改用攻击者身份与未签发租约，验证无权用户无法把公开对象 ID 升级为音频访问；持有已签发高熵租约仍按 Nginx 清空身份头后的能力凭据合同处理
- 新增候选 Clippy 零警告结果；本地合成运行复核通过浏览器 durable ACK 与安全停止、对象租约授权、工作区树、第二设备 `409`、租约接管、旧租约拒绝和 SSE 游标重放
- 本批修订已补齐 Android `testDebugUnitTest` 与 `assembleDebug`，41 个实际任务全部通过；默认临时目录曾触发 Java loopback 故障，改用独立短路径临时目录后完成构建，没有把主机故障误记为代码失败
- 最终文件树已重新执行隔离安全认证和 PR 全量 diff 自审；认证仅绑定该精确候选，后续修改仍不得复用
- VPS 只读回读确认继续运行 `quality-v24-20260831`：183／183 个发布文件与私有归档源精确匹配，Core healthy、重启计数 0、根分区可用 56 GiB；本批未创建 v25，也未重复部署 v24

## 0.5 2026-08-31 收尾状态校正

- 当前运行版本继续保持 `quality-v24-20260831`，本次只同步脱敏源码和状态文档，不进行无差异重复部署
- 统一纯文本迁移后缀、调整一个局部参数名，并用等价 TOML／YAML 表达保留锁定依赖；这些是公开候选审计兼容性修正，不改变迁移内容、接口或运行行为
- Gradle Wrapper 改为提交可扫描的 Base64 文本，并在启动时离线解码到用户缓存、核对固定 SHA-256 后执行；Gradle 分发版本和 Wrapper 字节不变
- 精确公开候选已通过 Rust 45 项、Python 29 项、Web 21 项以及 Android 单元测试与 debug assemble；类型检查、Ruff、mypy、ESLint、生产构建和 `git diff --check` 同时通过
- 90 分钟真实链路门保持通过；6 小时长测保持失败，不以恢复后的局部结果覆盖 Core 访问冲突和最终失败任务
- 24 小时低负载监测已经中断且没有正式结果，后续在 6 小时门通过后使用新的私有结果路径重新执行
- Android 前台、锁屏、ACK 清理和页面安全停止已有证据；通知栏停止、来电、蓝牙、Wi-Fi、耗电以及 MacBook 真实麦克风仍待验证

## 0.3.1 2026-08-31 浏览器音频 WebSocket 身份修正

- 修复浏览器取得录音租约后连接音频 WebSocket 时被错误返回 `trusted authentication proxy is required` 的问题
- WebSocket 现在验证已签发且绑定项目与会话的租约令牌，并恢复对应项目所有者身份
- Android 继续使用设备 Bearer 凭证，普通 API 仍要求可信 Authentik 代理
- Core 身份单元测试通过，`quality-v24-20260831` 已部署，并经 `wss://live.aialra.online` 公网路径收到带 `commit_id` 的 durable ACK；线上真实 MacBook 麦克风仍待硬件标签验收

## 0.3 2026-08-31 浏览器优先录音与工作区可用性修正

- 浏览器录音改为先验证本机音频输入，再取得项目租约，权限拒绝、无设备和租约冲突都会在录音卡片内立即显示
- 增加连接超时、异常关闭、断线恢复和 durable ACK 的可见状态，停止时会等待尾音封存和全部音频块确认
- AudioWorklet 改为零增益输出，避免麦克风回放啸叫；共享音频路径独立使用 `getDisplayMedia`
- 安卓入口改为默认收起的备用收音说明，浏览器有麦克风时不再依赖安卓配对
- 固定系统笔记改用用户语言显示，保留内部映射，并在课程文档标题下解释自动整理与我的笔记边界
- 创建课程按钮、输入占位文字、禁用状态、焦点样式和深色模式对比度完成修正
- Web 类型检查、单元测试、ESLint 和生产构建通过；`quality-v24-20260831` 已部署到 `live.aialra.online`，现有浏览器标签已验证工作区文案、录音阶段即时反馈和无麦克风时的超时提示；MacBook 外部 Chrome 因 CDP 返回空标签，仍待真实硬件收音验收

## 0.2 2026-08-31 当前提交 90 分钟正式门完成

- 受控 HTTPS 音频经真实 WSS、VPS SQLite、私有 GPU Worker 和 ReadWeave 连接器完成 90 分钟运行
- 5374／5374 个音频块确认，1478 个声学片段整理为 403 个连贯段落和 403 个译文，63 个教学块，最终总结 1 份
- 三次客户端断网均恢复，正常 ACK p95 为 152 毫秒，全量 ACK p95 为 203 毫秒；ASR、翻译、讲解和总结端到端 p95 为 1306、2508、5321 和 20398 毫秒
- 重复段落、重复译文、失败任务和 GPU OOM 均为 0，最终状态为 `session.completed`；原始结果文件已保存到冷存储，SHA-256 为 `620AA916F5BE116365190DB0407C64437AEBC9F8FD169BA2353BD6105459D91C`
- Android 设备路由已在公网 TLS 代理中隔离 Authentik 页面，现有前台服务已完成真实声学收音和 CUDA 结果验证
- 解锁后已自动切回 AIALRA 执行安全停止；Android 真机确认 815 个音频块、33 个连贯段落、33 个译文、3 个教学块和 1 份 14B 总结，327 个模型任务全部完成，会话进入 `completed`
- Android ACK 校验拆出纯值合同，修复 JVM 单元测试调用 Android `JSONObject` 桩方法导致的误报；Debug 与 Release 单元测试现已全部通过

## 0.1 2026-08-31 6 小时隔离长测最终读回

- 6 小时目标会话已停止并完成 SQLite 只读复核, 共 21324 个音频块, 重复 0, 序号缺口 0, 5864 个稳定片段, 1600 个译文和 83 个讲解卡
- Provider 仍为 `faster-whisper:small@cuda` 与 `ollama:qwen2.5:7b-instruct@cuda`, GPU OOM 为 0
- 该轮发生一次 Windows Core 访问冲突并恢复, 另有 1 个翻译任务最终以 `provider_response_invalid` 失败, 会话状态为 `failed`, 发布门明确保持未通过
- 已将验证器输出保存到冷存储证据目录, 不把恢复成功误写成无故障通过
- 修复线上 Nginx 将 Android 一次性配对交换误导向 Authentik 登录页的问题, 现已热加载精确公开路径并用新配对码读回 `200 application/json`; 其他 API 和内部路径仍保持保护

## 0 2026-08-31 线上 ReadWeave、ACK 与长测执行读回

- 通过受控输入替换 VPS Core 的 ReadWeave ETAPI Secret，Core 私有地址和公开跳转地址保持分离，ReadWeave 容器未重启
- 线上 ETAPI 完成真实根节点读取、验证节点 revision、管理标记保护、人工内容保护、标记缺失冲突、离线恢复和连接器幂等验证，公网 `/etapi/` 继续返回 `404`
- 发现线上旧 Core 的 `audio.ack` 缺少 `commit_id`，保留失败证据后部署当前提交，重新读回 ACK 已包含稳定提交标识
- 线上 RTX 4080 Agent 通过 Tailscale Worker Gateway 上线，读回 CUDA ASR、7B 本地翻译和 GPU 遥测
- 当前提交的 90 分钟网络音频正式门已重新启动，使用真实 HTTPS、WSS、SQLite、模型和 ReadWeave，结果生成前不提前宣称通过
- 原隔离 6 小时长测记录到一次 Windows Core 访问冲突，已保留 WER 并在原数据目录恢复，恢复运行不等于无故障通过
- Android 已发现真机但当前仍锁屏，MacBook 当前标签等待 Authentik 登录，设备发布门继续保持未关闭

## 1 2026-08-31 P0 证据账本与当前发布门修正

### 1.1 结果提交与音频持久化

- 模型完成请求现在必须同时带任务幂等键、租约持有者和运行证明，Core 会核对 Provider、执行设备、模型标识和时间窗口
- 内部模型对象读取现在必须带当前租约 Worker ID，缺少身份或使用其他 Worker 时分别返回 `401` 和 `409`
- 14B 总结和图片解析进入独占 GPU 调度，不与 ASR 或其他大模型并行加载，避免 16 GB 显存竞争导致随机 OOM
- `model.job.completed` 事件保存经过校验的 Provider、执行设备和运行证明时间，便于反查真实运行位置
- 音频 manifest 与 `audio.chunk.received` 在同一 SQLite 事务提交，相同序号和相同内容重传返回同一个 commit ID，内容或元数据改变时显式冲突
- 事件 ID 重复时同时校验 schema、事件类型、来源、序号、correlation 和 causation，禁止静默改变事件血缘
- 课程总结失败进入可见的 `session.summary.failed` 状态，保留实时事实并支持原任务幂等重试；网页不显示假总结
- 最终总结 Worker 超时收紧为 120 秒，超过异步门禁会进入可见失败并保留手动重试，不再让一次 14B 请求无限占住队列
- 课程总结段落现在同时显示概览、关键知识点、术语和待复习问题，术语不再只存在于 ReadWeave 投影
- 浏览器停止录音时把 DingTalk A1 视为可选备份控制，A1 暂时失败也不会阻塞主录音的尾音封存和服务器停止动作
- Android 一次性配对交换现在明确列为唯一免 Authentik 的生产入口，其他 API 仍强制受信代理身份和项目归属检查
- 内部恢复与对象授权检查支持按 `job_id` 定向领取任务，共享队列中的旧任务不会再污染授权门禁
- SQLite 验证器支持按 `--session-id` 固定复核目标会话，后续短测不会改变已归档长测的验证结果
- 网络音频长测工具增加显式 `AIALRA_ALLOW_EXTENDED_SOAK=true` 门槛，可运行 6 小时或 24 小时而不会让普通短测误启动超长任务
- Nginx Core 与 ReadWeave 代理模板先清空客户端身份头，再写入 Authentik 可信身份，降低代理配置遗漏导致的伪造风险
- 长测结果可通过 `AIALRA_SOAK_RESULT_PATH` 持久化为独立 JSON，便于在进程退出后复核指标而不把课程内容写入日志
- 已启动隔离 Core、模型端口、GPU Agent 和数据目录的 6 小时真实网络音频门，当前只记录运行中状态，结束后再以脚本与 SQLite 双重复核
- 新增低负载运行时 soak 工具，24 小时只检查 Core、GPU Agent、队列和安全遥测，不采集课程内容，也不创建模型任务
- 已同时启动 24 小时低负载运行监测，结果写入冷存储证据目录，结束后与 6 小时长测一起复核

### 1.2 当前真实证据

- 项目同步、单录音租约、乱序重传、Core 重启、安全边界和浏览器双设备短测通过
- 最新运行证明 1 分钟网络音频完成 60/60 ACK，正常网络 ACK p95 为 7 毫秒，断网影响 ACK p95 为 5014 毫秒，ASR p95 为 1420 毫秒，翻译 p95 为 1624 毫秒，14B 总结为 16798 毫秒，GPU OOM 和重复段落为 0
- 30 分钟网络音频预检完成 1782/1782 ACK，三次断网恢复，134 个连贯段落、134 个译文和 22 个教学块，ASR p95 为 474 毫秒，翻译 p95 为 1492 毫秒，讲解 p95 为 3046 毫秒，14B 总结为 18737 毫秒，重复段落、GPU OOM 和假结果为 0
- 最新 90 分钟正式网络音频完成 5345／5345 个 ACK，三次断网恢复，401 个连贯段落和译文，66 个教学块与 1 份 14B 总结；正常 ACK p95 为 3 毫秒，ASR、翻译、讲解和总结 Provider p95 为 429、1341、2781 和 17049 毫秒，重复、失败、GPU OOM 和假结果均为 0
- 6 小时和 24 小时发布门尚未按当前运行证明协议执行
- 总结结果增加重复术语、关键点和复习问题的稳定去重，避免模型重复输出造成阅读噪声；该补丁随后通过 25 项 Python Worker 测试
- 之前一次 90 分钟运行的最终总结约 291 秒，超过同步和异步预算，已降级为失败证据

### 1.3 维护说明

- 旧版本中的长测通过数字继续保留为历史记录，不再代表当前发布签字
- 下一次长测必须把结果文件、数据库校验和运行配置一起归档，不能只保留摘要数字

## 2 2026-08-30 连贯段落、强模型与 MacBook 质量回归

### 1.1 连贯课程内容与稳定模型链路

- ASR 短窗口结果改为内部声学片段，服务端按终止标点、长度和片段数量追加生成 `paragraph.finalized` 连贯段落
- 每个连贯段落只创建一项翻译任务，翻译提示词使用前两个段落解决术语和指代，但禁止重复翻译上下文
- 自动讲解从每五个片段触发改为至少四个新段落、累计 900 字符且实时队列排空后触发
- 一次讲解结果只显示为一个教学块，块内区分要点、背景、术语、疑似听写、复习问题和课件证据
- 停止录音后从持久音频块封存尾音，段落和译文排空后只创建一项最终课程总结
- 默认模型升级为 7B 段落翻译、7B 滚动讲解和 14B 最终总结，生产结果继续强制校验 CUDA Provider
- 模型调用增加有限超时和重试，Provider 不可用时保留可重试任务，不返回假译文或占位总结
- ReadWeave 转写与预览改用连贯段落，旧会话仍兼容原有字幕事件

### 1.2 工作区与同页交互

- Authentik 登录后直接进入 `/app`，刷新和深链接恢复项目或课程会话，不再显示一次性营销入口
- 新增用户文件夹、项目、课程会话、固定系统笔记、归档和设备偏好，工作区树与 ReadWeave 保持稳定节点映射
- 字幕改为连续课程段落，支持双语、仅原文和仅译文，Provider、耗时、证据和修订可追踪
- 讲解完整显示本段总结、背景补充、术语解释、疑似听写、复习问题和课件证据
- 长课程默认只渲染最近 160 项内容，用户可以分批加载历史，事件库和 ReadWeave 仍保留完整记录
- 麦克风测试在本地完成静音基线、语音峰值和削波检查，不取得录音租约，也不上传测试音频
- 状态颜色只表达运行语义，绿色表示可用或已同步，黄色表示排队或恢复，红色表示录音、离线、冲突或失败
- Service Worker 只缓存工作区外壳和静态资源，不缓存 API、身份路由、SSE 或音频请求
- 观察设备明确显示录音由另一台设备控制，不再显示不可用的“继续收音”按钮
- 已完成会话重新打开时，SSE 历史回放不再把状态短暂倒退为录音中，状态只能沿 ready、recording、processing、completed 单调前进
- README 页面验收脚本改为验证 `/app` 工作区、连续段落、独立讲解、真实 GPU、占位文字对比度和当前完成状态，不再依赖已删除的入口页选择器

### 1.3 分层模型与 GPU 遥测

- `faster-whisper small + CUDA float16` 处理实时 ASR，7B 处理段落翻译与滚动讲解，14B 只处理停止后的最终总结
- Qwen3-VL 8B 处理图片 OCR 与视觉解释，只在实时队列空闲时运行，并与其他大模型按需卸载
- 14B 最终总结在 V17 与 V18 生产回归中分别于 18.356 秒和 16.019 秒完成，并保持为默认总结模型
- 最终总结从整场字幕、滚动总结和材料页均匀取样并保持时间顺序，不再只读取课程开头
- 修复 Core 最多发送 48 个滚动总结而 Worker 只接受 32 个的契约偏差，避免长课程在模型调用前被 `422` 拒绝
- GPU Agent 不再把大模型切换期间实时模型暂时退出显存误判为 Worker 故障，模型切换不再导致代理进程退出
- GPU Agent 每 5 秒上报利用率、显存、功耗、温度、当前模型、任务类型和队列深度，不上报主机名、设备序列号或进程列表
- small CUDA ASR 在 7B 讲解运行期间保持独立并发，真实并发探针的 ASR p95 为 602 毫秒
- 发布候选保留早期 7B 任务粒度过细导致翻译 p95 超标的失败证据；改为段落级翻译与讲解节流后，V17 三十分钟 7B 翻译 p95 降至 1.078 秒
- 后台总结完成后恢复 7B 常驻，下一次实时翻译不承担模型冷启动
- Windows 守护脚本现在主动启动并监控 Ollama；Ollama 连续 3 次失联时重启受控本地模型栈
- 安装器同时验证 7B、14B 和 Qwen3-VL 8B，缺少任一生产默认模型时停止安装
- 修复安装后立即启动仍继承旧 3B 进程变量的问题；当前启动项、本机健康接口、Ollama 驻留模型和线上 Worker 心跳均读回 7B CUDA

### 1.4 Android 与 ReadWeave

- Android 使用一次性配对码取得受限设备凭据，令牌经 Windows 或 Android 平台安全存储，不再手填项目和会话标识
- ReadWeave 为工作区文件夹、项目、会话和 5 类系统笔记保留稳定 note ID，移动节点不重建远端笔记
- 页面中的 ReadWeave 图标使用当前标签跳转，浏览器仍无法取得 ETAPI Token
- ReadWeave 断线验证中，同一连接器任务经过 4 次尝试后完成，排队数从 1 恢复到 0，7 个映射节点全部回到 `synced`

### 1.5 当前真实门禁

- v17 五分钟生产预检完成 295／295 个 ACK，82 个内部片段形成 23 个连贯段落和 23 个译文，只生成 3 个教学块与 1 份 14B 总结
- 5 秒和 15 秒完整客户端断网全部恢复，段落与译文重复为 0，GPU OOM 为 0
- ASR、7B 翻译、7B 讲解和 14B 总结 p95 分别为 1.227、1.126、4.214 和 26.676 秒
- Core 在未满窗口时重启，两个已确认音频块恢复为一个段落、一项译文和一份 14B 总结，尾音与最终事件没有重复
- ReadWeave 的七个稳定节点全部回到 `synced`
- v17 三十分钟正式轮次完成 1777／1777 个 ACK，489 个内部片段整理为 134 个段落和 134 个译文，只生成 22 个教学块与 1 份 14B 总结
- 三次完整客户端断网全部恢复，ASR、翻译、讲解和总结分别为 0.914、1.078、4.194 和 18.356 秒，重复、OOM 和假结果均为 0
- v18 增加 7B 保守原文整理与自然意译，省略号不再被当成完整句末；原始 ASR 片段和原段落继续保留为追加式证据
- v18 五分钟生产回归完成 298／298 个 ACK，23 个段落全部具有整理后原文和译文，ASR、翻译、讲解和总结分别为 0.770、1.256、2.549 和 16.019 秒
- 最终自动检查通过 Rust 41 项、Web 18 项、Python 24 项，以及中英文 README 严格审核和四种视口主题组合

- 30 分钟完整客户端断网门禁完成 1762／1762 个 ACK、485 条字幕与译文、90 张讲解卡和 1 份 7B 总结，41 次续期传输失败全部恢复，缺口、重复、假结果和 OOM 均为 0
- 完整断网门禁的 ASR、翻译和讲解 p95 分别为 2.336、5.319 和 7.751 秒，均通过冻结门禁
- 新 90 分钟真实 GPU 门禁完成 5328 个 ACK、1466 条字幕、1466 条译文和 278 张讲解卡，三次网络中断后音频缺口、重复、假结果和 OOM 均为 0
- 90 分钟 Provider p95 为 ASR 1.284 秒、翻译 7.886 秒、讲解 10.678 秒，全部通过冻结门禁
- 30 分钟网络音频完成 1782 个 ACK、490 条字幕、490 条译文和 96 张讲解卡，音频缺口、重复字幕和 GPU OOM 均为 0
- 30 分钟 Provider p95 为 ASR 982 毫秒、翻译 6083 毫秒、讲解 9316 毫秒，全部通过冻结门禁
- GPU 离线恢复验证确认 756 个已确认音频块最终形成 208 条字幕、208 条译文、17 张讲解卡和 1 份课程总结，435 个任务全部只执行 1 次
- 多设备门禁确认第二台录音设备返回 `409`，45 秒后接管使 generation 从 1 递增到 2，旧租约上传被拒绝
- 自动检查通过 Rust 33 项、Web 16 项、Python 21 项和 Android 41 个构建任务
- v16 已部署到不可变发布目录，线上单页工作区、Authentik 边界、私有 Worker、ReadWeave 公网隔离和真实 GPU 遥测均完成读回
- 生产审计发现的 13 条早期 fake／fallback 合成验证事件已先做可恢复在线备份，再定向移除，对应 ReadWeave 投影重新校对后无排队任务

## 3 2026-08-29 长时恢复与线上安全

### 2.1 浏览器与私网恢复

- 浏览器重连后最多保持 8 个音频帧在途，ACK 到达后再发送下一帧，避免长时断线后一次性淹没 WebSocket
- 网络音频门禁同时切断音频 WebSocket 与 Core HTTP，原录音设备恢复时先续租、再重建 WebSocket，避免只验证音频连接重置却遗漏租约过期
- 浏览器进度同时检查本地采集序号和服务器确认序号，ACK 连续停滞会主动失败并保留证据
- SSH 转发增加 keepalive、失败退出和 1 秒监督重连，替代静默失效的单次转发进程
- 真实故障矩阵覆盖 Core 重启、GPU Agent 重启、Worker Gateway 中断、ReadWeave 离线、乱序上传、精确重传和第二设备接管

### 2.2 本机模型与启动

- base CPU 长测确认恢复稳定后，`small + CPU int8` 的 8 秒直接基准 p95 为 2.211 秒
- small CPU 与 3B CUDA 独立 30 分钟门禁完成 1183 个音频块和 394 个模型任务，ASR 端到端 p95 为 2.739 秒，成为当前默认组合
- CPU ASR 使用 12 个线程，模型 Worker 使用 Windows `AboveNormal` 调度优先级
- Agent 启动前依次预热 ASR、Ollama 和 ASR，确认模型真实可用后才领取生产任务
- 安装器验证系统 Ollama 中存在目标模型，并持久保存已验证的回环地址和模型名
- 健康检查要求配置模型至少 90% 驻留 GPU，不能仅凭 Ollama 进程存在声称 CUDA 可用

### 2.3 ReadWeave 与公开入口

- ReadWeave 页面接入统一 Authentik 登录，匿名访问返回登录跳转
- 公网 `/etapi/` 固定返回 `404`，Core 继续通过 VPS 私有回环或容器网络访问 ETAPI
- 新增脱敏 ReadWeave Nginx 模板，不在仓库保存真实域名、地址或证书路径

### 2.4 验证工具

- 长测数据库报告增加最近 20 个任务的 Provider 和端到端 p95，以及当前任务状态计数
- CPU ASR 基准支持显式模型选择，只输出耗时和非空计数，不输出转写内容
- 生产依赖审计、Rust 29 项测试、Web 13 项测试、Python 16 项测试、Android 完整构建和 DingTalk 小程序测试通过

### 2.5 README 与公开发布

- README 首屏统一居中，正文保持左对齐，并替换为真实模型链路生成的黑白页面截图
- 截图隐藏运行标识和时间，移除图片元数据，并通过亮色、暗色和窄屏浏览器检查
- 公开候选分别通过精确工作树和完整公开历史安全门禁，两次结果均为 `pass / allow`
- 公开 `main` 使用普通快进提交更新，发布后读回确认提交、Git 树、可见性和 README 均与门禁候选一致

## 4 2026-08-28 项目同步、录音租约与 ReadWeave

### 3.1 项目和多设备

- 新增 Authentik UID 所有的项目、项目会话和统一更新游标
- 新增项目列表、详情、修改、会话和 SSE 恢复接口
- 同一用户的多台设备共享会话状态和完整时间线，不同用户严格隔离
- Nginx 删除客户端身份头，只把受信代理返回的稳定 UID 写给 Core

### 3.2 单录音设备租约

- 新增 45 秒项目录音租约、10 秒续期、generation 接管和明确的冲突响应
- 允许过期但尚未被其他设备接管的原录音设备使用同一密钥续回，以补传长时间断网期间的缓存
- 音频 WebSocket 使用子协议传递租约密钥，旧租约和旧 generation 不能继续上传
- 租约续期响应只返回公开状态，不向浏览器返回服务端保存的令牌摘要
- Web 和 Android 都在录音前取得租约，停止时先排空已接受音频再释放租约
- 浏览器刷新和短时断网后从 IndexedDB 恢复未确认帧及后续序号

### 3.3 持久音频装配

- 新增音频块、装配游标和窗口组成范围的持久记录
- ACK 改为只在对象存储和 SQLite 事务完成后返回
- Core 重启后扫描未装配块，停止时从数据库封存尾部
- 修复重复课程音频被内容哈希误去重的问题，ASR 幂等键加入会话、来源和采集时间
- 新增首帧缺失、乱序补传、相同重传和冲突重传测试

### 3.4 ReadWeave

- 新增持久连接器任务和本地对象到远端 note ID 的稳定映射
- 自动建立课程概览、实时转写与翻译、生僻词与补充解释、课件证据索引和用户笔记
- 写入前创建 revision 并重新读取远端，只替换 AIALRA 管理标记内的内容
- 人工区域和用户笔记永不覆盖，标记缺失时建立恢复笔记并显示冲突
- 后续成功同步会清除当前冲突状态，历史冲突仍保留在项目更新记录

### 3.5 GPU 调度

- ASR、翻译和讲解拆分为独立领取通道
- 14B 在 30 分钟数据库门禁中因长讲解阻塞翻译而失败，该结果保留但不计入通过
- 7B 真实并发预检通过后成为默认翻译和讲解模型
- 5 分钟线上预检完成 298／298 ACK、60 条字幕、60 条译文和 12 张讲解卡，端到端 p95 均通过冻结门禁
- Ollama 双并发预检完成 297／297 ACK，Worker、ASR、翻译和讲解端到端 p95 均通过
- 30 分钟网络音频完成 1785／1785 ACK、357 条字幕、357 条译文和 71 张讲解卡，三次断网恢复、重复 0、OOM 0

### 3.6 页面与客户端

- 单页工作台加入项目选择、录音设备状态、同步观察和 ReadWeave 状态及预览
- 页面保持黑、白和中性灰，红色只用于录音、停止、拒绝和错误
- Android 缓存按租约 generation 隔离，停止动作等待未确认帧全部 ACK 后再释放服务器租约
- 生产构建仍不注册测试音源、演示路由或默认 mock

### 3.7 依赖安全

- 更新兼容范围内的 Rust 锁定依赖，生产前端审计和 RustSec 审计均无已知漏洞
- Core 限制 glibc 分配 arena 数并降低空闲页回收阈值，避免长时间 ReadWeave 全量投影把临时内存长期保留在多个工作线程

## 5 2026-08-27 真实 GPU 混合链路

### 4.1 持久任务与恢复

- 新增 SQLite `model_jobs` 与 `worker_nodes`，支持优先级、租约、续租、重试、幂等键和重启恢复
- 音频窗口落盘后只创建 ASR 任务，ASR 完成后创建翻译任务，讲解与材料任务不阻塞实时字幕
- 停止录音时封存尾部音频，会话使用 `processing` 区分录音停止与模型完成
- GPU 离线时继续 ACK 和持久排队，不生成 mock、identity 或 deterministic 结果

### 4.2 私有 GPU Agent

- 新增私有 Worker Gateway、心跳、长轮询、任务下载、完成和失败接口
- 新增 Windows RTX GPU Agent，ASR 与 LLM 使用独立执行通道
- Worker 明文令牌使用 DPAPI 保存，服务器只保留 SHA-256 摘要
- Agent 支持退避重连、60 秒租约、20 秒续租和登录自启动
- VPS CPU Worker 与默认 Ollama 服务退出当前生产主路径

### 4.3 浏览器与视觉

- 麦克风采集改为 AudioWorklet，未确认音频写入 IndexedDB 并在恢复后重发
- 新增浏览器标签页与系统共享音频入口，并保留来源 ID
- 删除生产默认演示开关与 `/demo` 路由
- 页面改为黑白层级，红色只用于录音、停止、权限拒绝和错误
- GPU 状态、队列深度、处理位置和真实 Provider 进入同一课程页面

### 4.4 验证

- 本机真实 GPU：21／21 ACK，4 字幕，4 译文，1 材料页，1 讲解卡，14.201 秒
- 私有远程 GPU：21／21 ACK，4 字幕，4 译文，1 材料页，1 讲解卡，35.015 秒
- GPU 离线恢复：离线时 0 假字幕与 5 个排队任务，恢复后重复字幕为 0
- Core 重启恢复：任务与 `processing` 状态保留，恢复后重复字幕为 0
- 浏览器桌面、暗色与 390 px 窄屏通过，控制台错误与警告为 0

## 6 2026-08-26 浏览器与 VPS 展示入口

### 5.1 新增

- 浏览器正式收音增加 HTTPS 安全环境检查、麦克风设备选择和服务器 ACK 状态
- 健康接口公开部署模式、处理位置与 Worker 能力，界面不再把 VPS 错写为本机
- 新增 Core、Worker、CPU Ollama 的 Compose 部署包
- 新增 Nginx WebSocket/SSE、Authentik 共享网关和 Cloudflare 代理 DNS 自动化
- 新增中英文 README 和浏览器/VPS 上线方案

### 5.2 安全调整

- 移除网页远程字体请求，避免无关第三方资源访问
- 移除核心服务的宽泛跨域允许，生产浏览器保持同源请求
- 生产容器只映射回环端口，数据和模型目录与代码版本分离

### 5.3 验证

- Rust 10 项测试、格式检查和 `clippy -D warnings` 通过
- Python Ruff、mypy 和 6 项测试通过
- 网页 ESLint、TypeScript、4 项测试和生产构建通过
- Compose 配置解析和 Cloudflare DNS 工具语法检查通过

### 5.4 线上部署

- Cloudflare 代理 DNS、Let's Encrypt 证书、Nginx WebSocket 反向代理和共享 Authentik 应用已部署
- 三项容器只把核心端口映射到 VPS 回环地址，匿名请求和伪造身份头请求均返回登录跳转
- VPS 默认 ASR 从 `small` 调整为 `tiny + CPU int8`，并关闭 Hugging Face Xet 下载路径，避免首次模型下载阻塞展示验收
- 部署器增加 Linux 行尾约束、本机探针代理绕过和 Nginx 重载后的有限重试，两次失败部署均自动回滚
- 最终 VPS 合成端到端验收通过：13 个音频块全部确认，产生 3 条字幕、3 条译文、1 页材料和 1 张讲解卡

## 7 2026-08-25 真机迭代

### 6.1 修复

- ASR 完成后立即释放优先许可，避免本地翻译或周期讲解阻塞后续音频识别
- Android 活动页面恢复时读取前台收音状态，持续收音时显示停止按钮
- 通知栏加入明确的停止录音命令，应用内停止按钮复用同一命令
- 根据核心配置记录，固定 4 秒 ASR 窗口改为短暂停顿结算与最长 8 秒保护，减少句子跨窗口切断

### 6.2 验证

- Android 15 真机的型号和系统版本保留在私有测试记录，两轮用户确认的 USB 本地收音测试完成后手机未确认缓存为零
- 根据本轮自动化记录，Rust 核心格式检查与 2 项音频单元测试通过
- 根据本轮自动化记录，Android Kotlin 单元测试与调试 APK 构建通过
- 根据本轮最终真机自检记录，应用内停止在接收 3 个音频块后完成核心会话并清空服务登记与手机缓存

### 6.3 待验证

- 新版静音分段与通知栏停止命令需要在真机完成下一轮受控朗读和通知栏动作复测
- 短句识别仍有词错，不能作为课程现场翻译质量通过的证据

## 8 2026-08-24 初始纵向切片

本次实施日志记录的变更日期为 2026-08-24

### 7.1 新增

- 建立 Rust 核心、事件协议、SQLite WAL 和内容寻址对象存储
- 建立 Python 本地模型与材料工作进程
- 建立 React 课程控制台、SSE 回放、材料上传和讲解卡
- 建立 Kotlin Android 长时录音客户端并产出调试 APK
- 建立 DingTalk A1 控制、服务器探针和小程序前台录音帧探针
- 核心服务配置文件设定每 5 条稳定字幕生成一次周期讲解
- 加入图片原图时间线预览
- 加入一键启动和安全停止脚本
- 加入标准决策审计、ADR、隐私边界和真机手册

### 7.2 修复

- 加入项目本地 NVIDIA CUDA 动态库路径，修复 `cublas64_12.dll` 缺失
- 停止会话改为排空已经接受的模型任务后再完成
- 修复 SSE 完成事件与停止响应之间的界面状态竞争
- 修复网页事件字段和 Rust 序列化合同不一致
- 加入内联图标，消除浏览器 `favicon.ico` 的 `404` 错误

### 7.3 已知未完成

- 记录于验证计划的 A1 真机、Android 真机和 90 分钟真实课程
- Tauri 安装壳、导出、删除与保留期界面
- OCR、视觉语言模型、嵌入检索和摄像头
