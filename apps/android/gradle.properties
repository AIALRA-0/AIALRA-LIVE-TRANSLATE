# Gradle receives enough heap for Android resource processing without consuming the model GPU budget.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# AndroidX provides the activity and lifecycle compatibility layers.
android.useAndroidX=true
# Kotlin formatting follows the official style used by automated checks.
kotlin.code.style=official
