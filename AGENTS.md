# AIALRA-LIVE-TRANSLATE Agent Rules

- 所有用户可见中文遵守本会话提供的 `$human-readable-technical-writing` 规范
- 录音、转写、课件、令牌和临时下载地址属于敏感数据，禁止写入 Git、测试快照或普通日志
- 真实录音模式必须先记录许可确认，并持续显示录音状态
- 本地模式是默认值，任何云端文本或图片出口都必须经过服务端策略检查
- 音频接收、持久化、确认和停止控制拥有最高优先级，模型任务不得阻塞这些路径
- 片段、译文、解释和人工修改采用追加式事件，禁止静默覆盖历史
- DingTalk 产品侧实时状态只能证明设备状态，第三方实时音频能力需要真机和开放接口证据
- 用户界面改动需要运行真实浏览器验证，Android 录音改动需要记录真机型号、系统版本和时长
- 有意义的工程改动同步更新 `PROJECT_STATE.md`、`docs/CHANGELOG.md`、`docs/KNOWN_ISSUES.md` 和测试记录

