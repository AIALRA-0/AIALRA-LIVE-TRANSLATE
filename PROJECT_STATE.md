# AIALRA-LIVE-TRANSLATE 项目状态

## 1 状态摘要

状态日期：2026-08-29

当前版本：`0.3.0` 项目同步与 ReadWeave 投影

当前结论：项目所有权、多设备同步、单录音设备租约、持久音频恢复和 ReadWeave 文本投影已经进入真实链路验证

生产结构由 Authentik 提供稳定用户标识，浏览器或 Android 先取得项目录音租约，再上传音频，VPS 完成持久化和 ACK 后把任务交给本机 RTX 4080

GPU 离线时不会生成占位字幕，音频和任务会保留在服务器，ReadWeave 离线也不会阻塞录音、转写或翻译

## 2 本轮完成范围

### 2.1 项目与身份

- 新增用户所有的项目、项目会话和统一项目更新游标
- 生产只接受反向代理写入的稳定 Authentik UID，本地开发固定使用 `local-user`
- 同一用户在多台设备看到相同的项目、会话、录音状态、时间线和 ReadWeave 状态
- 不同用户不能读取或修改彼此项目
- 历史会话通过显式迁移归入默认项目，不允许项目列表隐式认领

### 2.2 单设备录音

- 同一项目同时只有一个有效录音租约
- 租约每 10 秒续期，有效期 45 秒，第二台设备申请时返回 `409 recording_lease_conflict`
- 原设备在租约有效期内可以恢复，租约过期后新设备接管并递增 generation
- 超过 45 秒的网络中断若尚未发生接管，原设备可用同一密钥续回；一旦其他设备先接管，旧密钥永久失效
- WebSocket 密钥放在子协议头，不进入 URL、普通日志或时间线
- 旧租约和旧 generation 不能继续上传音频
- 停止操作封存尾部、释放租约，并把状态同步给所有观察设备

### 2.3 音频持久化与恢复

- ACK 只在音频对象和 SQLite 事务完成后返回
- 音频块、装配游标和窗口组成范围均持久化
- 后台装配器能在 Core 重启后重建未完成窗口和停止时的尾部
- 首帧缺失时装配器等待补传，乱序到达后按连续序号恢复
- 相同序号的重传必须与已提交内容一致，冲突重传会被拒绝
- ASR 幂等键同时包含会话、来源、采集时间和内容哈希，重复课程声音不会误合并
- 浏览器 IndexedDB 原子保存音频帧和下一个序号，刷新后不会重复使用旧序号

### 2.4 ReadWeave

- AIALRA 事件库是事实源，ReadWeave 是可重建文本投影
- 每个项目和会话自动建立课程概览、转写与翻译、生僻词与补充解释、课件证据索引和用户笔记
- 实时更新按批次进入持久连接器队列，停止后执行最终校对
- 每次修改前创建 revision，并在写入前重新读取远端内容
- 只替换 AIALRA 管理标记内的区域，标记外人工内容和 `99 我的笔记` 永不覆盖
- 标记缺失或远端冲突时停止覆盖并建立恢复笔记
- 后续人工修复并成功同步后，页面清除当前冲突状态，同时保留历史更新记录
- 浏览器只能看到同步状态和预览，永远不能取得 ETAPI Token

### 2.5 本机模型调度

- ASR、翻译和讲解使用独立任务领取通道
- 14B 模型在 30 分钟测试中暴露长讲解阻塞翻译的问题，没有成为默认值
- 7B 真实并发曾通过 30 分钟门禁，但与 GPU ASR 长时争用显存，不再是当前默认组合
- 当前默认 Provider 为 `faster-whisper:small@cpu` 和 `ollama:qwen2.5:3b-instruct@cuda`
- ASR 使用 12 个 CPU 线程，Windows Worker 使用 `AboveNormal` 调度优先级，用户程序不会被结束或降级
- Agent 启动前完成 ASR 和 Ollama 预热，安装器先验证系统 Ollama 中存在 3B 模型

## 3 当前验证状态

已通过：

- 5 分钟线上双并发调度预检：297／297 ACK，60 条字幕，60 条译文，11 张讲解卡
- 5 分钟数据库门禁：音频缺口 0、重复字幕 0、失败任务 0、GPU OOM 0
- 5 分钟延迟：Worker 领取 p95 0.250 秒，ASR 端到端 1.699 秒，翻译端到端 1.442 秒，讲解端到端 13.249 秒
- 30 分钟网络音频：1785／1785 ACK，357 条字幕，357 条译文，71 张讲解卡，3 次断网全部恢复
- 30 分钟数据库门禁：音频缺口 0、重复字幕 0、失败任务 0、GPU OOM 0
- 30 分钟端到端延迟：Worker 领取 p95 0.254 秒，ASR 2.111 秒，翻译 1.321 秒，讲解 15.801 秒
- 当前 small CPU 与 3B CUDA 30 分钟门禁：1183 块音频、179 条字幕、179 条译文、35 张讲解卡，缺口、重复、失败和 OOM 均为 0
- 当前 30 分钟端到端延迟：Worker 0.246 秒，ASR 2.739 秒，翻译 1.209 秒，讲解 2.297 秒
- 90 分钟完整性与恢复：4573 块音频、686 条字幕、686 条译文和 136 张讲解卡，经历四类故障注入后缺口、重复、失败和 OOM 均为 0
- Web：ESLint、TypeScript、13 项测试和生产构建通过
- Rust：格式、`clippy -D warnings` 和 29 项测试通过
- Python：Ruff、mypy 和项目锁定环境中的 16 项测试通过
- DingTalk 小程序：1 项测试通过
- Android：单元测试、Debug APK、Release APK 和 Release lint 通过

保留的失败证据：

- 第一轮完整 90 分钟浏览器运行在末段遭遇 SSH 转发静默断开，浏览器仍保留未确认音频，但一次性重发过多帧导致恢复停滞，该轮按失败保存
- 浏览器重发改为最多 8 帧在途，ACK 后再发送下一帧，SSH 转发增加 keepalive 和自动重连监督
- 第一轮 90 分钟测试在早期发现重复音频被内容哈希错误去重，2020 个音频块形成 404 个窗口却只有 4 个 ASR 完成，该结果无效且未计入通过
- 修复 ASR 幂等键后，第一轮 30 分钟应用层结果完整，但数据库门禁发现 Worker 领取、翻译和讲解端到端延迟超标，该结果未计入最终通过
- 第二轮 90 分钟运行中的外部高 CPU 程序造成累计 ASR 延迟超标，该轮只验证完整性和恢复，不会计入性能通过
- Worker 调度优先级修订后，最近 20 个任务已经恢复到门禁以内，仍需独立 30 分钟无干扰重跑

## 4 当前限制

- 90 分钟完整性与恢复门禁通过，累计性能因前半段外部 CPU 争用失败
- 准确率更高的 small CPU 与 3B CUDA 已通过独立 30 分钟性能门禁并成为当前默认配置
- 浏览器前台录音可用，移动浏览器锁屏和后台录音仍受操作系统限制
- Android 已支持项目租约和 generation，但原生 Authentik 登录仍未接入，生产首选入口仍是受保护浏览器
- Android 锁屏、来电、蓝牙、Wi-Fi 切换和耗电仍需新一轮真机长测
- DingTalk 公开资料仍未证明第三方连续 PCM 或增量逐字稿能力
- 复杂 OCR、公式、图表、VLM、摄像头和自动截图尚未进入生产链路
- 第一版只支持同一 Authentik 用户的多设备访问，不包含项目成员和多人权限
- 原始音频保留期、删除、导出和备份界面尚未完成

## 5 下一门禁

1. 读取当前 35 分钟资源报告并确认内存没有持续无上限增长
2. 生成新的真实页面截图，完成中英文 README 亮色、暗色、窄屏渲染和隐私检查
3. 通过 GitHub 安全发布门禁后，把精确发布提交快进推送到公开 `main`
