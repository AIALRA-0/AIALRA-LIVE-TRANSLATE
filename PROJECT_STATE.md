# AIALRA-LIVE-TRANSLATE 项目状态

## 2026-09-10 v37 已上线并通过生产闭环

当前生产版本为 `quality-v37-20260910-b4b419a`，运行代码 SHA 为 `b4b419a7775731a062bf29850cf5df1db211e905`。发布目录 `BUILD_ID`、Core `build_id` 和镜像 OCI revision 完全一致；Core 为 `running/healthy`，restart count 为 `0`，GPU Worker 在线，队列和有效录音租约均已排空。

本批把实时识别拆成可替换的临时预览和完整稳定段落：临时预览约每 3 秒更新、最长回看 12 秒，稳定窗口按语音边界在 6 至 24 秒内封存。只有稳定段落进入翻译、内容分组和总结，避免半句话反复触发下游任务。翻译继续使用 HY-MT，并加入受限的前文参考和同语言混合文字保护；源文仍为不可修改事实。说话人观察覆盖更长音频并保持不确定时不强行命名。

讲解链路仍严格校验总结的来源覆盖、语言和段落绑定；若模型修复后只有个别术语引文无法逐字核对，系统保留已经合格的总结，并只丢弃无法验证的术语项，避免整张内容卡片失败。第一版 v36 的生产冒烟正是因此失败；修复后相同失败输入已复测成功，v37 的两次独立生产冒烟均通过。

新代码验证通过 Rust workspace 95 项、Python 全量测试和静态检查、Web 61 项测试、TypeScript、ESLint、生产构建、PowerShell 语法检查及真实 Edge 的桌面／390 像素预览替换回归。两次 v37 生产冒烟分别约 75.9 秒和 53.4 秒，均完成 21 个带 `commit_id` 的音频确认、同项目第二设备 `409`、稳定识别、稳定翻译、材料解析、自动讲解、ReadWeave 读回、停止和队列排空，没有新增最终失败任务或 Core 重启。

当前版本已达到交给用户进行真实课程复测的工程条件。尚未由自动化证明的内容包括 macOS 物理麦克风与 EE680 课堂体感、自然多人说话人准确率、固定困难集 99% 准确率、PanelTone 的跨产品 GPU 仲裁，以及 6 小时／24 小时和 Android 真机验证。实时页面现在是“临时原文预览＋稳定段落替换”，译文仍在稳定段落形成后产生，并非逐字流式翻译。

## 2026-09-09 发布审核与共享显卡复测

本批代码准备直接同步 main，生产仍保留 v35，不能把源码同步当作新版已上线

已完成的工程验证为 Rust 93 项、Python 135 项、Web 60 项、真实 Edge 20 项与 12 个持久确认，Python 本轮重跑及静态检查通过，其余复用未变代码的既有结果

本轮额外双项目回放保存 128 块、4,096,000 字节，停止分别为 22／11 毫秒，租约均释放；之后翻译与讲解出现重复超时，队列未排空，已结束该私有回放，不将其计为通过

当时显卡约使用 15.2／16 GB，另有其他项目模型进程，未停止其他项目；仅凭这个同时出现的现象不能断定唯一根因，需在可比较的资源条件下定位超时，再进行生产切换

临时数值字面量检查会误拒绝正常的文字数值翻译，已撤回，没有用简易字符检查冒充事实忠实度验证；错词传播、讲解事实关系和自然多人课程效果仍需完成验收

采用 github-safe-publish 2.0.1 审核实际新增内容，未发现凭据、真实课程标识、私有路径或媒体模型文件进入本次改动，GitHub 写入限定为 origin/main 普通推送，不创建其他远端对象

## 2026-09-09 受限文字对照完成（最新，未发布）

用户明确同意后，两组课程文字共七段通过 Router 作为机密离线任务处理，实际模型 GPT-5.6 Sol，约 217 秒完成，未发送音频、账号或真实课程标识，结果只保存在私有目录

同输入对照仍不能恢复缺失的声学信息，能更明确区分不清楚的源文和可支持的解释，但翻译仍受错误听写限制，不作为线上实时模型替换依据

固定专业词提示另做 48 次公开样本和真实课程前中后 24 窗对照：公开样本词编辑数 31 增至 38，七项退步、零项改善，未合入默认识别路径，课程中的个别词改善不掩盖其他改动风险

开始测试前发现原模型进程已不存在，根因未确认；已从固定 v35 等价源码和原配置恢复，并使用独立隐藏进程启动，隔离测试后再次恢复成功，Core healthy、重启 0、无有效租约或待处理任务，线上版本未变

当前候选仍未提交、推送或部署，工程修复证据和内容质量未完成项分别保留；下方授权等待与进行中记录为历史断点

## 2026-09-09 原文预览与说话人窗口联动（最新断点，未发布）

已保存的识别小段现在先显示为原文预览，正式段落引用后自动收拢，不重复显示、不增加模型调用；用户上滑时保持阅读位置，返回底部后继续跟随，译文到达也可触发跟随

修复识别最长 16 秒与说话人检测最长 10 秒不一致的问题，长输入分成两个重叠检测窗，覆盖尾部，换人或重叠仍保留待确认，不强行分配编号

最新 Python 135 项、Web 60 项及对应静态检查与网页构建通过，真实 Edge 20 项、12 个持久确认通过，包含迟到译文滚动竞争的复现修复和同语言译文视图内容保留；固定公开的 12 条独立语音中有 6 条超过 10 秒，说话人观察均成功、匹配错误 0，CPU 处理 p95 399 毫秒，不作为自然多人会议准确率证明

生成讲解的中文标点与已有双语术语格式已统一，原始听写、引语、代码、数学和数字保持不改写；格式通过不代表语义通过，真实课程的错词传播及讲解补全错误仍是未关闭项，线上保持 v35，未提交或部署

用户确认后，已通过 Router 提交两组课程文字的离线对照，按机密任务处理，仅含源文和局部序号，不含音频、账号或真实课程标识；实际模型为 GPT-5.6 Sol，结果待复核，未改变生产云端策略

## 2026-09-09 独立模型回归（当前断点，未发布）

排除了旧守护程序自动重启造成的双份模型驻留，候选在单套模型环境下重跑 120 秒主课程与 8 秒并行项目，128 个音频块、4,096,000 字节全部确认，37 项任务均一次完成，停止分别为 29／21 毫秒，租约与队列最终为空

识别和翻译任务 p95 为 1.561／1.795 秒，讲解为 9.210 秒，不包含采集封窗和页面显示；此前受双份模型挤占影响的慢速测试不能作为正常性能结果

测试结束已恢复与线上 v35 对应的固定 Worker／Agent 代码和原配置，Core 仍 healthy、重启数 0、无活动录音或模型任务，未改生产课程数据，未提交、推送或部署候选

逐段内容复核仍发现识别错词及其向翻译、讲解传播的问题，继续同音频的有限模型对照，不以无任务失败代替内容通过，下方为历史快照

## 2026-09-09 术语背景与来源复核（当前断点，未发布）

补充：定位到 10 毫秒静音尾块绕过静音检查、触发旧模型接口 503，候选移除为单元测试保留的短输入放行逻辑，短静音不再进入模型，有声音的弱尾音保留，HTTP 回归通过，最新 Python 全量 131 项及静态检查通过

同一固定 24 条公开音频的原音／弱声共 48 次对照中，16 秒上限加短静音保护组合的词编辑数为 31，此前 8 秒候选为 43，11 项改善、0 项退步，音频样本完整保留，模型非空请求 p95 2.274 秒，不含最多 16 秒采集等待，不能宣称页面 3 秒实时

已在本地候选接入 16 秒上限，停顿仍可在 4 秒后封段，停止立即封存不足 4 秒的尾部，Rust 93 项及格式／Clippy／构建通过，当前正在重新运行双项目真实模型短回归，之前的整课证据不覆盖本次窗口变化

已获用户授权，通过受限 Router 任务校核 14 个通用公开概念，仅发送去标识的通用术语问题，不发送课程、音频、听写或材料，实际使用 GPT-5.6 Sol，单次任务约 169 秒，作为离线资料参考，不进入实时处理链路

凭据仅保存在仓库外当前 Windows 用户保护的秘密存储中，未进入 Git 或测试输出，生产仍为 v35，本轮没有提交、推送或部署

将有限的公开资料核对结果整理为 13 类背景定义，按原词出现和上下文领域匹配，网页明确区分背景资料与老师原话，并提供受约束的公开出处链接，未覆盖术语仍走模型，不能据此宣称通用定义质量已经解决

最新 Rust 93 项、Python 全量 126 项、Web 56 项及对应静态检查通过，真实 Edge 16 项和 12 个持久确认通过，包括桌面／390 像素窄屏的背景提示与安全链接

固定真实来源的首组讲解现可减少至一次本地模型调用，但仍发现听写错误向讲解传播、残缺数字被补成确定结论，结构通过不等于内容通过，正在进行来源约束对照，未扩大生产出口或替换线上模型

下方内容均为此前断点快照，其中“尚未获 Router 授权”和较早测试数量已由本节更新，不能当作当前状态

## 2026-09-09 整课回放与输入修复（最新断点，未部署）

整节 2,192.379 秒课程按双倍输入速度回放，并行另一个项目，2,201 个音频块逐个持久确认，564 个模型任务均一次完成，两个课程停止、重复停止、租约释放及最终队列排空通过，Core 保持运行，原生产模型已恢复

主项目 ACK p95 为 57.4 ms、停止 78 ms，另一个项目停止 14 ms；ASR 任务 p95 为 1.730 秒、翻译 3.082 秒，均为隔离回放的任务指标，不冒充浏览器端到端延迟，ReadWeave 未包含在此测试

浏览器采集补上原生抗混叠滤波，避免降采样把目标频带以外的高频折入人声，44.1 kHz 分块重采样同时修正终点与相位误差，真实 Edge 的八种已知频率输入均保留精确样本数，高频折叠相对原实现降低至少 37.8 dB，尚未据此宣称 ASR 识别率提升

最新 Web 55 项、真实 Edge 16 项和 12 个持久确认通过；Python 全量 120 项及 Ruff、mypy 通过，类型、ESLint、构建和 diff 检查通过

整课旧讲解任务 p95 达 141.1 秒，发现讲解上下文在 Worker 内未传入实际请求，已修正，并将同主题相邻源段按容量合并生成，保留逐源引用，固定主课程首组复测 6 次调用、约 19.2 秒，仍有定义错义，不能用速度改善替代内容质量

向 ASR 注入此前识别文本的实验出现静音重复旧话，未采用；14B 术语对照仍有错误，未替换；已询问用户是否允许只校核去标识术语与通用定义的受限 Router 出口，尚未获答复、未发送任何内容，本地回归继续

最新合并讲解版本的双项目短回归通过：128 个音频确认、39 项任务、停止 13／17 ms、租约和队列最终为空，讲解任务 p95 约 18.6 秒，原生产模型已恢复，生产 Core healthy、重启数 0，未提交、推送或部署，当前无遗留测试进程

## 2026-09-09 原音一致性与真实音频复核（最新断点，未部署）

候选默认保留原音，不再默认启用 GTCRN，麦克风测试与录音共用处理设置，显式选择本机降噪时不再叠加浏览器自动增益或回声处理，加载失败不偷偷切换另一种降噪

当前 Python 全量 114 项、Web 53 项通过，Ruff、mypy、类型、ESLint、构建和 diff 检查通过，最新真实 Edge 16 项、12 个持久确认通过，包含原音测试、主动选择 GTCRN、设备释放和同课程续录

真实 GPU 的双项目 120 秒回放完成 128 个音频确认和 39 个后台任务，队列排空、停止及租约释放通过，但课程原文／译文复核仍发现术语、数字和断句问题，因此不把工程成功记成内容质量通过，未发布候选

独立比较 Qwen3-ASR 与已有 Whisper large-v3 的同音频、8／16／30 秒窗口，尚未证明 Whisper 值得全面替换，长窗口也未消除错误，未更改线上识别模型，整节约 37 分钟音频的加速双项目回放正在执行

术语定义仍有事实性错误，新增提示词、自我复核和当前预算下的推理模式均未通过完整资格检查，不并入正式生成路径，也不将没有独立参考稿的课程比较写成 WER

## 2026-09-09 当前进展（候选未部署）

最新增量：语义主题判断、匿名说话人、分批讲解和完整课程汇集已接入本地候选，仍未提交、推送或部署，线上保持 v35

Rust workspace 92 项通过；最新 Python 108 项通过，静态检查继续随改动执行；Web 51 项通过。真实 Edge 验证 15 项、12 个持久确认，包含无限次续录的关键场景，以及合成事件驱动的说话人／同主题续接展示，后者不冒充真实人物识别验证

实际模型验证：Qwen3.5-9B 上下文翻译在 17 条合成输入中保持源文不变；ASR 共存热请求约 0.630–0.898 秒，尚不是页面端到端指标。主题新留出样本加入边界复核后通过，CPU 说话人候选在 20 个清晰窗口中无已分配时间误分，混说与重叠返回待确认。以上均未替代完整长课验收

单次完整讲解仍有漏项、错误定义和长时间占用，已改为逐个受限调用并允许翻译插队；中间结果不发布，全部来源和术语完成后才形成内容组。最终课程汇集不再抽样 64 段／24 组，而是复用有完整来源契约的内容组，补齐未覆盖原文和材料。该新路径正在实际模型验证，不能记为已经通过

同课程反复续录及停止恢复仍保留在本次修复中，原质量计划没有中断。最新 Rust 84 项、Python 74 项及静态检查通过；Web 当前代码此前 49 项通过，事件重试修复的真实 Edge 回归验证 12 个 ACK、停止恢复、续录和多段讲解换行

新增回归复现并修复：同一稳定事件重试时重新分配顺序号，导致正常重试被当成冲突；现在复用原事件的顺序和时间，内容或来源不同仍拒绝，覆盖并发及重启恢复。翻译原文从已保存任务绑定，不再允许模型改写

同一批 24 条公开参考音频的原音／弱声共 48 次比较，4 秒最短窗口、8 秒最长窗口的词编辑数由 v35 的 61 降至 43，请求数由 163 降至 77；仍有个别样本退步。已作为本地候选接入，停止仍立即封存短尾音，逐字节完整性测试及新 Core 的真实 Edge 全流程回归通过；不将离线请求时长冒充真实课堂延迟

整句上下文离线对比的总词编辑数由 61 降到 33，但等待整句不是实时方案，继续验证稳定提交策略。现有 7B 讲解仍漏事实，14B 完整输出两次超过 75 秒；语义分组和说话人首个候选也有明确误分，均未切换线上。Qwen3.5-4B 讲解更完整，但仍有错误术语；15 条合成翻译测试发现先后关系颠倒，二次校对也未消除，未采用该方案或修改生产配置。完整结果见验证报告

## 2026-09-08 EE680 当前执行断点（未发布）

原计划继续，加入同课程无限次续录，不因 Mac 忘记停止而缩减质量修复。续录、独立观察端与旧事件／旧总结隔离已通过 Rust 和真实 Edge 回归；Rust 79 项、Web 49 项。当前增加模型完整结束检查及讲解来源核验，Python 65 项、Ruff、mypy 通过。

公开独立参考对比发现弱声切片候选改善弱声，但原音略有退步；Silero 候选和首个 CPU 说话人候选也未满足整体替换要求。上下文翻译比独立片段在部分承接上更好，但术语和单位关系仍需验证修复。未将这些候选上线，未更换生产模型；下一步继续识别上下文、翻译契约和完整讲解的同输入验证。

## 2026-09-08 用户补充与续录候选

用户查明 Mac 上另一页面忘记停止录音；此前持续音频与续租不能据此判定为停止故障。后续只读检查有效租约与活动队列均为 0，Core healthy、重启数 0；不改库，不替用户结束课程。

候选前后端允许已完成课程反复续录，保留旧内容；停止后保留代次并作废旧令牌，防止跨设备音频编号重用；完成事件和总结按录音轮次区分。通用状态转换仍拒绝后台直接重新开启课程，只有显式获取租约才能恢复。三轮连续开始／停止测试通过；真实浏览器和后续质量计划继续执行，尚未部署。

## 2026-09-08 EE680 审计与停止恢复修复（本地候选，未部署）

用户确认点击过“停止并完成处理”。另一条未完成课程没有停止／处理／完成事件；最终只读复核有效租约 0、活动任务 0，已保存及已组装音频均为 3,152 秒，Core healthy、重启数 0。持久状态仍为 recording，不能据此判断用户正在收音。没有修改生产数据库或代替用户结束课程。

本地修复把停止意图提前到点击时保存：先停输入、封存尾音，失败后只补传缓存；同一标签页刷新保留待完成停止。续回原持有者租约后可补传，不获取新代次或抢占其他设备；停止响应丢失可幂等重试。停止和续租请求增加有限超时，状态查询及连接回调不得将停止恢复成收音。工作区的当前录音指示改为有效租约投影，不改写历史会话。

Rust 77 项、Web 48 项，以及格式、Clippy、类型、ESLint、构建通过。真实 Edge 验证停止请求断网、音频补传断网、刷新与租约到期、停止响应丢失和其他设备接管；正常停止与缓存补传均验证持久确认，重试未再次请求麦克风。桌面及 390px 页面通过。证据为私有 `browser-stop-recovery/browser-validation.json`，非生产 GPU 或物理手机验收。

EE680 长课另确认识别窗口过碎、固定 16 段分组、讲解过短和最终总结失败；弱声误切保护已有本地测试与固定一分钟推理对比，尚无独立参考稿证明识别率提升。连续翻译、语义分组、完整讲解和说话人分离仍按 [EE680 计划](docs/EE680_QUALITY_PLAN.md) 执行，不能记成已经完成。线上仍为下述 v35；本轮尚未提交、推送或部署。

## 2026-09-08 当前线上 v35：内容分组、断句与人声保真

已部署 `quality-v35-20260908-c6d95b1`，运行 SHA `c6d95b16cced9295c93914f9bf78b4c2802e2ac8`；BUILD_ID、Core build_id 和 OCI revision 一致。Core healthy、重启数 0，GPU Worker 在线，未重启或修改 Worker；v34 回滚资源保留。

内容组移到 GPU 前，折叠原文、显示覆盖段数；未成组时显示积累内容，不再暗示逐段生成。自动讲解从 4 段／900 字符提高到 8 段／1800 字符，短句累计 16 段时形成一组；已整理内容不重复触发，自动选中的原文不再逐段截尾。

翻译保持 HY-MT 与当前段落隔离，一个长声学片段不再独自触发翻译，冒号／省略号不视为句末；保留四片段上限和停止尾段封存。默认降噪候选改为既有组件中的 GTCRN，浏览器本地运行，无新 GPU 模型或依赖；其他模式与关闭选项保留。没有证据支持盲目叠加自动 EQ。

Rust 76 项、Web 43 项及格式、Clippy、类型、lint、构建、真实浏览器检查通过。生产双项目冒烟约 108 秒通过：42/42 durable ACK、冲突 409、8 条稳定识别、4 条译文、材料讲解、ReadWeave、停止和队列排空；停止约 471／282 ms，模型失败增量 0，两个测试项目已归档。此次仅处理四项优化，交给用户体验；后续纯文档 HEAD 不重新部署相同运行代码。

## 2026-09-08 历史版本 v34，主流程验证通过

运行版本为 `quality-v34-20260908-6329980`，代码 SHA 为 `632998062dc4137e40201fb3dd09e2f2fdcf14ef`。发布目录 BUILD_ID、Core build_id 和镜像 OCI revision 一致。Core healthy、重启数 0；GPU Worker 在线，Qwen3-ASR-1.7B 与 HY-MT1.5-1.8B 均为 CUDA，实时模型已预热。旧版本与数据／配置备份保留。

本批修复历史租约误撤销、麦克风权限与资源释放、录音常亮生命周期、课程状态引导、讲解模型显存释放和推理忙碌重试。接入可关闭的本地 RNNoise；新建按钮仅创建项目；个人笔记支持追加保存和冲突保护，转写支持有证据音频回听，术语可以展开。指定公开视频的固定识别文本对比确认，自由历史上下文会把前文数量带入当前译文；生产翻译现只使用当前段落和显式术语约束。

最终工程检查通过：Rust 74 项、Python 51 项、Web 43 项，以及格式、Clippy、Ruff、mypy、TypeScript、ESLint、构建和差异检查。真实 Chromium 再次通过页面权限、历史事件、RNNoise 音频闭环、笔记及桌面／390px 验证。

最终生产双项目冒烟约 135 秒通过：42/42 durable ACK 含 commit_id，同项目第二设备 409，8 条稳定识别、4 条译文、材料自动讲解、笔记回听、ReadWeave、停止和队列排空通过。停止确认约 548／284 毫秒；测试对象已归档，活动租约、queued、leased 均为 0，累计失败数保持 121，Core 无新增重启。

仍需明确：后台整理两次复测约 82–260 秒，整理后的实时模型首轮重新加载仍约 7.5 秒 ASR、4–6 秒翻译；不能宣称始终无等待。公开视频一分钟的噪声模拟和短推理不是用户手机实测，也不是商业软件质量等价证明。Wake Lock 仍受操作系统限制，Paneltone 未被修改。当前交付为已通过主流程的可体验版本，不宣称绝对稳定。

本次最终状态同步仅修改文档，不重新部署；后续文档 HEAD 与以上运行 SHA 的差异应仅为四份状态文档。下方旧版本和候选章节为历史快照。

## 2026-09-08 录音与翻译可靠性修复过程（阶段快照）

首轮 v34 生产检查已通过双项目录音、字幕／译文、笔记和回听，但材料讲解出现长等待。确认讲解分支没有先释放专用 ASR／翻译模型，补齐与总结分支一致的显存释放，并新增调用顺序回归。此修订需要重新部署和完整冒烟，不能沿用首轮的部分通过结论。

随后生产复测通过，材料讲解降至约 10 秒。公开课程短片段的固定 ASR 对比又发现自由历史上下文把前文数量带入当前译文，因此本次继续移除该翻译输入，只保留当前完整段落和显式术语约束。公开样本和原文仅保存在仓库外私有目录；该修订待新 SHA 部署。

本批基于 v33 处理用户新反馈，不沿用 v33 的通过结论。修复历史租约 SSE 误判、权限准备期间误写音频、测试与录音抢占输入、迟到授权资源泄漏和常亮生命周期。未完成课程恢复不再等待翻译队列排空；已结束课程保留历史，并引导新建课程。

接入本地 RNNoise，保留原生降噪和关闭选项；HY-MT 使用官方专用模板，限制上下文及解码时间。Worker 在客户端取消后仍保持实际推理独占，忙碌请求在原任务租约内等待；修复专用模型显存释放、预热和本项目进程看门狗。“新建项目”只建项目；五个模块增加独立空态、个人笔记自动保存与版本冲突、音频证据回听和术语展开。

本地工程与 Chromium 验证已执行，尚需新 SHA 的远端部署和真实 GPU 冒烟。没有数据库迁移、其他项目操作或本地 Docker/WSL。选型及实测边界见 `docs/SPEECH_ENHANCEMENT_DECISION.md` 和 `docs/VALIDATION_REPORT.md`。

## 0.2 2026-09-07 录音入口、静音门与内容组讲解收敛（已部署）

当前运行版本为 `quality-v33-20260907-cb8564a`，运行代码 SHA 为 `cb8564a5b5380b03f974a3f59e8541a778c4c84e`。发布目录 `BUILD_ID`、Core `build_id` 和容器 OCI revision 完全一致；Core 为 `running/healthy`，restart count 为 `0`。GPU Worker 在线，ASR 为 `qwen3-asr:Qwen/Qwen3-ASR-1.7B@cuda`，实时翻译为 `hy-mt:tencent/HY-MT1.5-1.8B@cuda`，讲解为 `ollama:qwen2.5:7b-instruct@cuda`。

本批收敛了用户已复现的入口与内容问题：页面首次检查录音状态时不再闪红或伪装成权限等待；麦克风权限只从用户操作发起，设备刷新、录音开始和麦克风测试不再并发索取设备；麦克风测试改为相对噪声底的自适应判断；ASR 在 Provider 前拒绝没有可信语音包络的静音窗口；翻译清理并拒绝源语言、目标语言、术语背景和“翻译后的文本”等元数据泄漏；自动讲解按连续稳定内容组生成，并把“内容组总结”和专业名词／缩写的一句话解释放在侧栏，不再逐段高频生成。

新代码的本地快速验证全部通过：Rust workspace 70 项测试、格式和 Clippy；Python 48 项测试、Ruff 和 mypy；Web 37 项测试、TypeScript、ESLint 和生产构建；部署脚本 Bash 语法和 `git diff --check`。本地未启动 Docker、Docker Desktop 或 WSL，也未修改或停止 Paneltone。

生产合成冒烟约 77 秒通过：21/21 音频块收到带 `commit_id` 的 durable ACK，同项目第二设备返回 `409`，产生 4 条稳定字幕、2 条稳定译文、1 页材料抽取和 1 张自动讲解卡；安全停止、ReadWeave 读回、队列排空和测试对象归档完成。冒烟前后累计失败数保持 `110`，活动租约、queued 和 leased 最终均为 `0`，Core 无新增重启。公网未登录入口进入 Authentik，`/internal/` 返回 `404`，伪造跨域 Origin 返回 `403`。

真实 Chromium 的桌面与 390 像素检查通过：空闲录音状态不显示红色冲突，页面不会自动请求麦克风权限；根目录右键新建、独立拖拽手柄、主题切换、录音与材料入口、页面宽度边界和正文诊断过滤均正常。该自动化没有代替真实物理麦克风、静音房间、口音课程、内容质量和系统 Wake Lock 的用户验收；现在交给用户进行实机测试，后续只处理可复现的 P0/P1。

## 0.1 2026-09-06 翻译稳定性与 GPU 调度修复（已部署）

本批代码已部署到 `quality-v32-20260906-8e80efc`。GitHub `main`、发布目录 `BUILD_ID`、Core `build_id` 和容器 OCI revision 均为 `8e80efc1ee0cb10fc10298c668332f48a464d484`。Core 为 `running/healthy`，restart count 为 `0`；GPU Worker 在线，ASR、实时翻译和 Ollama 均证明使用 CUDA；活动租约、排队任务和执行中任务均为 `0`。

本批修复了翻译失败不应拖垮已保存课程、自动识别语言传递、翻译元数据清理、段落总结与知识补充分栏、黄色录音状态和 16 GB 显存默认串行保护。AIALRA 侧已避免 ASR 与翻译／Ollama 重叠，但当前仓库没有 Paneltone 代码或进程，因此没有宣称完成两个产品之间的主机级互斥。

生产合成冒烟第二次通过：21 个音频块全部收到带 `commit_id` 的 durable ACK，同项目第二设备返回 `409`，产生稳定字幕和译文，材料解析、自动讲解、ReadWeave 读回和队列排空通过。第一次冷启动冒烟在旧的 300 秒等待窗口内超时，随后同一版本热运行约 70 秒完整通过；这说明首轮失败是模型冷启动和串行调度造成的等待超时，不是最终模型失败。该结果不替代用户真实课程对延迟和翻译质量的判断。

当前交付状态：版本已通过技术健康门和合成生产冒烟，交给用户进行真实体验。真实课程翻译稳定性、长时间运行、屏幕唤醒、macOS 麦克风切换和 Paneltone 外部协调仍需实际验证；后续只处理真实复现的 P0/P1，不继续扩张功能范围。

## 0.0.9 2026-09-06 翻译稳定性与 GPU 调度修复（提交时待部署，后由 v32 部署）

本批运行代码已直接提交并推送到 GitHub `main`，SHA 为 `bfe40be9383b343dc4a3ffac8ee23474ff1394ca`。线上仍是上一版 `quality-v31-20260906-7e7df16`，本批尚未远程部署，因此不能把下面的本地结果当成线上结果。

本批针对最新课程反馈收敛了四个根因：翻译 HTTP／Provider 失败不再把已经保存的录音和稳定段落标成整节课程失败；自动识别会把 ASR 返回的语言归一化并按段落传给翻译，中文、英文、日文混合内容不再统一按固定源语言处理；翻译结果会清理泄漏的源语言、目标语言、术语和翻译标签；讲解改为“段落总结＋知识补充”，移除疑似听写、复习问题和置信度展示，并在右侧按当前可见段落切换。

录音中、收尾中和处理中状态统一使用黄色，红色只表示失败或不可继续。GPU Agent 的生产默认调度改为 ASR 与翻译／Ollama 不重叠，翻译仍有独立优先级并避免长期被 ASR 饥饿；安装脚本和启动脚本都把重叠设为显式 opt-in。这个门只覆盖 AIALRA 自己的任务通道，当前工作区没有 Paneltone 的进程或代码，不能据此宣称已经完成两个产品之间的主机级互斥。

本地验证已通过：Rust workspace 测试、Clippy、格式检查，Model Worker 与 GPU Agent 共 41 项测试、Ruff、mypy，Web 33 项测试、TypeScript、ESLint、生产构建、PowerShell 语法解析和 `git diff --check`。没有启动本地 Docker、Docker Desktop 或 WSL。下一步只有远程部署该 SHA 后做健康核对和一次生产最小冒烟，再交给用户真实体验；专用模型质量、真实课程延迟、Paneltone 外部协调和长期稳定性仍不能由本地测试代替。

## 0.0.8 2026-09-06 v31 专用实时模型接入与最小生产冒烟（当前线上）

当前唯一有效线上快照：线上运行代码 SHA、发布目录 `BUILD_ID`、Core `build_id` 和容器 OCI revision 均为 `7e7df161934537fcbfddd04e7cb5f971481dbef9`；线上发布为 `quality-v31-20260906-7e7df16`。部署完成后 `main` 只追加了本次脱敏状态文档，本次没有创建分支或 PR，也没有启动本地 Docker、Docker Desktop 或 WSL。

实时模型组合已切换为 `Qwen/Qwen3-ASR-1.7B@cuda` 和 `tencent/HY-MT1.5-1.8B@cuda`。讲解和总结继续使用既有的本机 CUDA Ollama 路径。专用模型的首次加载会明显增加首个请求耗时，加载完成后进入热运行；GPU 不可用时不会静默回退到 CPU。

本次先发现一个真实兼容问题：专用 Worker 已经完成 CUDA 推理，但 Core 结果门仍只接受旧的 `faster-whisper:` 和 `ollama:` provider，导致第一次生产冒烟的 ASR 结果被 Core 拒收。已在 `main` 只修正 provider 兼容门，保留旧 provider、同语言 identity 和现有队列／音频协议；Rust provider 回归测试、workspace 测试、Clippy、格式检查和 `git diff --check` 均通过。

修复后的生产合成冒烟通过（约 163 秒，包含首次模型热身）：21/21 音频块收到带 `commit_id` 的 durable ACK；同项目第二设备返回 `409`；产生 5 条稳定字幕、2 条稳定译文、1 页材料抽取、1 张自动讲解卡；安全停止、ReadWeave 读回、队列排空和测试对象归档通过。成功冒烟没有新增最终模型失败。

远端健康核对通过：Core `running/healthy`，restart count `0`；GPU Worker 在线；ASR provider 为 `qwen3-asr:Qwen/Qwen3-ASR-1.7B@cuda`，实时翻译 provider 为 `hy-mt:tencent/HY-MT1.5-1.8B@cuda`，讲解 provider 为 `ollama:qwen2.5:7b-instruct@cuda`；活动租约 `0`，模型队列 `queued=0`、`leased=0`。前一次失败冒烟产生的累计失败计数属于测试失败历史，不作为当前运行任务判断。

当前判断：v31 已完成“专用实时模型接入＋技术冒烟”并交给用户体验。此前私有困难语音集没有达到原先的 99% 硬指标，本轮按用户决定先以真实体感判断，不把该指标伪装成已通过。真实课程的识别漏词、翻译逻辑、延迟、总结质量、屏幕常亮、macOS 物理麦克风切换和长期稳定性仍需用户确认；后续只处理真实复现的 P0/P1，停止未经反馈的功能扩张。

## 0.0.6 2026-09-05 v29 体验收敛修复（上一线上快照）

本次运行代码继承上一提交的模型收敛修复：记录模型阶段耗时；ASR 使用有界解码和最近稳定上下文；翻译契约区分源/目标语言并处理同语言场景；总结保留课程首尾和中间边界；讲解证据可回到对应时间线；课程文档有滚动边界和新内容提示；录音期间申请屏幕唤醒锁。随后只对 `tools/e2e_smoke.mjs` 增加与浏览器一致的 10 秒录音租约续借，避免异步材料讲解等待造成测试工具自身的租约过期假阴性。

本地快速验证实际结果：Rust workspace 66 项测试、Clippy、格式检查、Python Worker 33 项测试、Worker Ruff、mypy、Web 28 项测试、TypeScript、ESLint、生产构建、Python 编译检查、Node 语法检查和 `git diff --check` 通过。全仓库 Ruff 仍有既有的 `tools/sqlite_validation_report.py` 单点规则问题；全仓库 pytest 的两个工具测试受当前环境缺少 pytest-asyncio 插件影响，均未被当作业务通过。远端 Docker 构建成功，未在本地构建容器。

部署后健康门通过：Core `running/healthy`，restart count `0`；GPU Worker 在线，ASR provider 为 `faster-whisper:small@cuda`，LLM provider 为 `ollama:qwen2.5:7b-instruct@cuda`；活动租约为 `0`，模型队列 `queued=0`、`leased=0`，累计失败计数仍为历史值 `98`，本次未增加。VPS 根分区仍有约 `55.5 GiB` 可用，v28 发布、镜像、数据库和配置备份继续保留为回滚目标。

最终生产合成冒烟通过（约 73 秒）：21/21 音频块收到 durable ACK 且均含 `commit_id`；同项目第二设备返回 `409`；产生 6 条稳定字幕、2 条稳定译文、1 页材料抽取、1 张自动讲解卡；安全停止、结果读取、ReadWeave 读回和队列排空通过。公网未认证健康入口返回 `302` 并进入 Authentik，公网 `/internal/` 返回 `404`。冒烟使用一次性合成材料，不证明真实课程的词错率、翻译逻辑、总结质量、屏幕常亮或用户操作体验。

冒烟过程中曾发现并修正一个测试工具问题：原工具在等待材料讲解时未续租，导致一次队列排空瞬间误判和一次 `recording_lease_expired`；补充续租后重新部署并通过。该问题不代表线上浏览器续租实现失败，浏览器原有续租周期仍为 10 秒。

当前判断：v29 已完成本批技术上线，可交给用户真实使用；模型延迟和内容质量只能通过带真实课程的用户反馈继续判断。6 小时/24 小时长测、真实 macOS 麦克风、Android 真机矩阵、Windows 崩溃调查和参考答案质量评测仍未执行，不能写成已通过。后续只处理真实复现的 P0/P1，停止未经反馈的功能扩张。

## 0.0.5 2026-09-05 体验收敛修复（已部署）

本次修复已直接提交并推送到 `main`，运行代码 SHA 为 `90a5b1ce04cc1ae28d9c8b94dda0a3b0ea5ab6b7`，并远端部署为 `quality-v29-20260905-90a5b1c`。改动针对本次课程审计暴露的收尾问题：停止后先完成音频和实时结果闭环，不让总结任务阻塞会话完成；模型翻译结果增加源语言、目标语言和避免原文照搬的契约检查；ASR 解码参数改为有界的可配置值；课程文档增加跟随底部和“有新内容”提示；桌面文档区域设定边界，窄屏保留页面滚动；录音期间申请屏幕唤醒锁并在页面返回时重试；时间线明确标记总结仍在后台生成。

本地候选已通过 Rust workspace、Model Worker、Web typecheck/Vitest/ESLint/生产构建、Python 编译检查、Rust 格式检查和 `git diff --check`。远端健康门和合成生产冒烟也已通过：Core healthy、restart count 为 0、GPU Worker 在线且 ASR/LLM 为 CUDA、队列为空；21/21 音频块收到带 `commit_id` 的 ACK，字幕、译文、材料解析、自动讲解、ReadWeave 读回和安全停止通过。真实浏览器麦克风、不同浏览器的屏幕唤醒能力和真实课程内容质量仍需用户体验确认。没有启动本地 Docker、Docker Desktop 或 WSL。

## 0.0.4 2026-09-05 v29 最小收尾状态（已上线）

- 运行代码 SHA 为 `5e72c836ca8f189122e3f5436e9eacf9fda1ea45`；该修复已直接提交并快进推送到 GitHub `main`，工作树在文档同步前保持干净，未启动本地 Docker、Docker Desktop 或 WSL。
- 当前线上为 `quality-v29-20260905-5e72c83`。发布目录 `BUILD_ID`、Core `build_id`、容器 OCI revision 和 GitHub `main` 运行代码 SHA 均为 `5e72c836ca8f189122e3f5436e9eacf9fda1ea45`。
- v29 Core 为 `running/healthy`，restart count 为 `0`；GPU Worker 连续探针在线，ASR 与 LLM provider 均为 CUDA，队列 `queued=0`、`leased=0`。
- v29 生产快速冒烟通过：租约、同项目第二设备 `409`、双项目并行租约、21/21 durable ACK（均含 `commit_id`）、稳定字幕、稳定译文、材料解析、确认后自动讲解、ReadWeave 读回、安全停止、重复停止和测试对象归档均通过。
- v29 首次部署健康超时已触发自动回滚；现场确认的最小代码修复是回滚时清除继承的候选 `AIALRA_BUILD_ID`，避免 v28 镜像与 v29 环境变量污染。修复后重新远端构建、切换和健康核对均通过；v28 release、镜像和备份仍保留为回滚目标。
- 公网边界快速核对通过：未认证健康接口返回 `401`，公网 `/internal/` 返回 `404`；数据库可读，活动租约为 `0`，Core 无新增重启。
- 6 小时、24 小时、Android 真机矩阵、MacBook 物理麦克风确认和 Windows 崩溃调查未执行；本次上线判定仅基于快速技术冒烟，不宣称长时稳定性或真实硬件验收完成。

## 1 当前结论

状态日期：2026-09-01

### feedback-11 录音状态、工作区与材料讲解（2026-09-01，历史提交时快照）

本节记录本批实现及提交时发布断点。PR #15 已 squash 合并为 `44a8dbfc036bfc769d8edc9fcaa3c23d0cfba3d1`；后续文档提交已将当前 `main` 推进到 `501143ae4439f666f7b5ed25a7acb87f39c08b2c`，以上方 2026-09-05 状态为准。

- 录音状态现在区分实际租约冲突、容量不足、可恢复历史、正在处理和不可继续录音。无活动租约且没有排队模型任务的 `recording`／`degraded` 会话只在用户确认后沿用原会话追加，历史按时间戳保留；本批不删除或自动修写过期租约记录。
- 工作区录音状态订阅 `workspace.recording.changed`，释放、到期和重新获取会清理旧冲突提示；不同项目仍可并行录音，容量门只影响新项目。
- 新建文件夹／项目入口收敛到根目录或文件夹右键菜单；拖拽改为独立手柄与明确的前面／里面／后面落点，服务端原子移动完成前不乐观改写树。回收站阻断会说明活动录音或处理中原因。
- 移除可见“课程笔记分类”标题，保留分类按钮；补齐侧栏主题变量、设置状态窗口和材料区域样式。
- “讲解与材料”已移到时间线之后。文件选择或拖放先进入确认框；只有确认后才保存并立即创建材料解析任务及一个等待解析完成的幂等讲解任务，连续确认上传合并到同一等待任务，解析失败时讲解不执行。
- Rust workspace 63 项测试、Clippy 和格式检查通过；Web 27 项测试、TypeScript、ESLint、生产构建和差异检查通过。Python 未改动，本批未重复执行其检查；未启动本地 Docker、Docker Desktop 或 WSL。
- 轻量脱敏复核、单提交、PR #15、squash merge、`main` 回读和短期分支清理已完成；远程 `quality-v29-20260901-44a8dbf` 尚未部署。部署前只读检查发现 VPS 根分区可用约 18.0 GiB、可用比例约 4%，未满足部署器的 20 GiB 或 5% 门槛，因此没有构建、切换或重启 GPU Agent。
- 提交时远程只读检查确认现有 `quality-v28-20260901-1849013` 健康；后续 2026-09-05 的部署尝试和回滚结果见上方当前状态，不重复解释为 v29 已上线。
- 真实浏览器复核和 v29 生产冒烟仍未完成。

### feedback-11 部署器回滚修正（2026-09-01，实现与验证）

- v28 首次切换因部署命令未显式传入现有 `.env` 中的数据目录和认证服务而安全失败；回滚后发现 symlink 已恢复 v27，但共用 `latest` 标签仍启动了 v28 镜像。
- 当前实现从 `.env` 安全读取已有数据目录和认证服务，构建前给旧镜像保留不可变 release 标签；失败时重新标记、强制重建旧容器并核对实际 image ID，不再把部分回滚报告为成功。
- 当前线上已通过显式正确参数完成 v28 部署；本候选只影响以后部署，不改变当前录音、模型或数据。

### feedback-10 窄屏运行状态窗口修正（2026-09-01，已部署 v28）

- v27 的真实 Chromium 复核发现 390 像素视口中，全屏运行状态窗口仍叠加遮罩的 16 像素内边距，导致左右越界。
- PR #13 已 squash 合并为 `1849013ce2e04dd8f098df6652b4e57462c0974c`，远程部署 `quality-v28-20260901-1849013`；Web 26 项、类型检查、ESLint、生产构建和部署后桌面／390 像素 Chromium 复核通过。

### feedback-09 多项目录音与界面收敛（2026-09-01，已部署 v27）

- 录音租约继续按项目隔离：同一项目仍只允许一个录音标签页，不同项目可以并行持有租约；浏览器录音身份改为标签页级，工作区偏好身份仍跨标签保存。
- 新增项目录音状态与容量判断。ASR Worker 离线、CUDA／ASR 降级或 ASR 积压超过 15 秒时只拒绝新的项目录音；已有录音的续租、恢复、ACK 和停止不受影响。
- 冲突状态会按服务端租约自动复查并在释放或到期后清除，不再永久停留在“另一台设备已接管”；项目和会话状态变化同时刷新工作区指示灯。
- 项目可设置以后课程使用的默认讲授语言和翻译语言，新建课程可逐节覆盖；已有课程语言保持不变。
- 工作区拖拽改为服务端原子移动，明确区分“放到前面、放入文件夹、放到后面”，课程会话禁止跨项目移动。
- 已移除可见的“课程笔记分类”标题；亮暗主题覆盖整个侧栏；设置窗口改为可滚动的大型分区面板，窄屏使用全屏布局；麦克风仍按项目保存具体设备选择。
- PR #12 已 squash 合并为 `c69384cb4e19bd38fa5e17d8862052bb36410c89`，并远程部署 `quality-v27-20260901-c69384c`；发布目录 `BUILD_ID`、Core `build_id` 和 OCI revision 一致，Core healthy、restart count 为 0。
- 生产合成链路约 81 秒通过：21／21 durable ACK 含 `commit_id`，第二设备 `409`，产生 6 条稳定字幕、2 条稳定译文、材料、讲解和 2 条 ReadWeave 可读结果。另一次双项目检查证明两个项目可同时持有租约，停止后状态立即清空。
- 未使用本地 Docker／Docker Desktop／WSL。真实 Chromium 桌面布局、语言默认值和侧栏主题通过；390 像素设置窗口越界已进入上方 feedback-10 最小修复。

### 0.0.2 用户体验反馈批次（2026-09-01，已部署）

- 针对拖拽误放，工作区现在只接受合法目标，并在拖动中显示正在移动的对象、当前高亮目标和“放这里”提示；非法目标不会触发移动。
- 项目概览优先显示可继续的未完成会话；再次进入同一会话会沿用已确认历史和租约恢复路径，时间线与会话头部显示本地时间戳；已完成会话仍不可覆盖。
- 麦克风设备在授权后重新枚举，显示浏览器返回的具体名称，保存用户选择并监听设备插拔；下一次录音连接和本地麦克风测试使用所选设备。
- Web 快速验证已通过：26 项单元测试、类型检查、ESLint、生产构建和 `git diff --check`；反馈实现经 PR #9 合并，录音设备刷新稳健性修正经 PR #10 squash 合并，运行源码为 `e2acd0190b72f173318afc223cfa231e4933ccda`。
- 已部署 `quality-v26-20260901-e2acd0`：发布目录 `BUILD_ID`、Core 健康响应 `build_id` 和 OCI revision 均为同一完整 SHA；Core healthy、restart count 为 0，queued／leased 模型任务为 0。
- 生产合成冒烟约 78 秒通过：21／21 音频块收到带 `commit_id` 的 durable ACK，第二设备返回 `409`，产生 6 条稳定字幕、2 条稳定译文、1 页材料抽取和 1 张讲解卡，安全停止后会话完成，ReadWeave 返回 2 条可读结果。
- 本批没有修改音频协议、服务端接口、数据库迁移、模型策略或 ReadWeave 数据；未启动本地 Docker／Docker Desktop／WSL。
- 真实登录浏览器的 macOS 物理麦克风型号显示与切换仍需用户在目标设备确认；页面不会伪造浏览器未提供的设备名称。

当前应用版本：`0.5.2`（本批未修改应用版本字段）

当前线上判断：`quality-v28-20260901-1849013` 已上线；v27 的生产合成音频与双项目租约短测继续有效，v28 仅追加窄屏 CSS 修复并已通过真实 Chromium 复核

当前发布门：`V28 ONLINE / TECHNICAL SMOKE PASSED`

### 0.0.1 2026-09-01 ReadWeave 持久化修复与生产读回（前一运行批次）

- 运行代码已在 PR #5 squash 合并为 `2c43fb82cf672538b5104bbf9211d4a67480eede` 并部署；随后 PR #6／#7 只追加脱敏状态文档，远端仍仅保留长期 `main`
- ReadWeave 管理边界改用可持久化的 HTML `data-*` 标记，同时兼容旧注释标记；ReadWeave 清理旧注释后，仅当正文仍与 AIALRA 生成内容一致时恢复标记，人工修改继续进入冲突保护
- 远端已部署 `quality-v25-20260901-2c43fb8`；发布目录 `BUILD_ID`、Core 健康响应 `build_id`、OCI revision 三者一致，Core 与 ReadWeave healthy，Core restart count 为 0
- 重启后的唯一 GPU stack 已恢复，Worker 健康检查为 ASR／Ollama 可用；线上 Provider 为 `faster-whisper:small@cuda` 和 `ollama:qwen2.5:7b-instruct@cuda`
- 第二次真实生产合成冒烟通过（约 79 秒）：21／21 音频块收到带 `commit_id` 的 durable ACK，第二设备返回 `409`，产生稳定字幕、译文、材料页和讲解卡，安全停止并完成会话，ReadWeave 读回 2 条结果；结束时活动租约和活动模型任务均为 0
- 新冒烟项目的 7 个 ReadWeave 映射全部保留管理边界和匿名对象标记；上一轮注释被清理导致的 7 条 `managed_region_conflict` 记录作为历史证据保留，没有删除或伪装成成功
- 本轮没有使用本地 Docker／Docker Desktop／WSL；仅在 VPS 按部署脚本进行远端候选构建。6 小时、24 小时、Android／MacBook 真机矩阵和 Windows 崩溃调查仍按用户范围未执行

### 0.0 v25 快速上线（已部署）

- 模型失败接口保持原有路由和必填字段，仅新增五值 `error_stage` 与固定格式 `diagnostic_id` 两个可选字段；旧客户端兼容，不新增数据库迁移
- GPU Agent 已按网关响应、任务载荷、模型 HTTP、模型 JSON 和执行设备分阶段报告，同一失败报告的传输重试复用诊断标识；日志不记录模型正文、转写、音频、令牌、真实会话 ID或完整载荷
- Core 健康响应新增 `build_id`，生产 Compose、发布目录 `BUILD_ID` 与 OCI revision 使用同一完整 `main` SHA；部署器新增磁盘、活动租约、队列、SQLite/WAL/SHM 备份及失败回滚检查
- 本轮快速检查已通过 Rust workspace 48 项、Clippy、Python 33 项、Ruff、mypy、Web 21 项、类型检查、ESLint、生产构建、Node 语法和 `git diff --check`；VPS Bash `-n` 也已通过
- GitHub 运行源码提交、发布目录 `BUILD_ID`、Core `build_id` 与 OCI revision 均为 `43a61adc2ef6c13c8b2bb96c67575dd3f56bbfc8`；Core healthy、重启计数 0，GPU Worker 在线且队列排空
- 生产合成音频冒烟在 60.866 秒内完成：第二设备返回 `409`，21／21 个音频块收到带 `commit_id` 的 durable ACK，生成 6 条稳定字幕、2 条稳定译文、1 页材料抽取和 1 张讲解卡，安全停止后会话完成，ReadWeave 可读

### 0.1 公开主干收敛兼容性

- 早期四个纯文本 SQLite 迁移与后续迁移统一使用 `.migration` 后缀，`include_str!` 引用同步更新；迁移内容和执行顺序不变
- ReadWeave 概览渲染的局部参数改用不与凭据规则冲突的名称；Python 与 pnpm 锁文件只调整等价序列化，依赖版本和制品摘要不变
- Android Gradle Wrapper 以可扫描的 Base64 文本保存；启动脚本每次执行都会复核缓存 JAR 的固定 SHA-256，摘要不一致时从已审计文本原子恢复，并原子刷新同版本 properties；不下载或替换 Wrapper
- 上一精确候选 `8ec1094` 已通过 Rust workspace 45 项、Python 29 项、Web 21 项、Android 单元测试与 debug assemble；2026-09-01 自审修订已重新通过 Rust 45 项、Python 29 项、Web 21 项、Ruff／mypy、类型检查／ESLint／生产构建、Clippy 零警告、Windows／POSIX Wrapper 缓存破坏恢复，以及 Android `testDebugUnitTest` 与 `assembleDebug` 41 个实际任务
- 2026-09-01 的本地合成运行复核已通过浏览器 durable ACK 与安全停止、对象租约授权、工作区树、单录音租约第二设备 `409`、租约接管、旧租约拒绝和 SSE 游标重放；IDOR WebSocket 用例已改为攻击者身份加未签发租约，避免把已签发高熵租约错误建模为公开对象 ID
- GitHub PR #2 已经完整 diff 自审、隔离签名认证并以 squash 方式合并；合并后 `main` 文件树与认证候选精确一致，短期分支已删除，旧 PR #1 已注明被取代并关闭
- 本地日常仓库已收敛为一个工作目录和一个跟踪 `origin/main` 的长期 `main`；旧私有 refs 只保留在已验证的离线 bundle，不进入 GitHub 历史
- VPS 已切换到 `quality-v25-20260901-43a61ad`；Core healthy、重启计数 0、私有健康接口正常、公开入口进入 Authentik、公开 `/internal/` 为 `404`

### 1.0 本轮浏览器优先录音修正（已部署）

- 浏览器录音现在先在本机取得并验证音频输入，再申请项目录音租约，麦克风权限失败或设备不可用时不会留下服务器租约
- 录音卡片新增准备、权限、租约、连接、收音、停止和模型处理阶段，连接失败、超时、网络恢复和租约接管都会在卡片顶部显示可读提示
- AudioWorklet 使用零增益输出，采集内容不会回放到扬声器；屏幕共享模式独立检查 `getDisplayMedia`，不再错误依赖麦克风接口
- 停止录音会先封存尾音并等待持久 ACK，短时连接中断时继续重连，只有全部音频块得到服务器确认才完成停止
- 安卓收音入口默认收起，并明确说明它只服务于没有本机麦克风的设备，不参与浏览器主录音链路
- 工作区固定节点隐藏数字前缀，改为课程概览、逐段转写与翻译、补充讲解与术语、课件与证据和我的笔记；内部 section key 与 ReadWeave note ID 保持不变
- 创建课程按钮、禁用状态、输入占位文字、深色模式和键盘焦点均补充了明确颜色与说明
- 本地 Web 类型检查、21 项单元测试、ESLint 和生产构建已通过；`quality-v24-20260831` 已部署到 `live.aialra.online`，现有浏览器标签已复核工作区节点、录音阶段反馈、权限超时提示和 Android 备用入口；MacBook 外部 Chrome 仍因远程 CDP 返回空标签而没有自动附着证据

### 1.0.1 浏览器音频 WebSocket 身份修正（已部署）

- 浏览器已经通过项目录音租约取得身份，但音频 WebSocket 经过安卓免登录代理位置时会被 Core 误判为缺少可信 Authentik 代理
- Core 现在会验证 WebSocket 子协议中的已签发租约令牌，并从租约绑定的项目恢复所有者身份；普通 API、项目权限和租约校验保持不变
- Android 继续使用原有设备 Bearer 凭证，未扩大设备凭证范围
- `quality-v24-20260831` 已部署，Core 身份单元测试和浏览器风格 WebSocket 验收均通过，测试租约经 `wss://live.aialra.online` 公网路径发送 1 个 PCM 音频块并收到带 `commit_id` 的 durable ACK，随后安全停止并归档测试项目
- 外部 MacBook Chrome 仍因远程 CDP 没有可附着标签而未完成真实硬件麦克风验收

### 1.1 本轮执行读回（最新，覆盖下方同日旧记录）

- ReadWeave 线上 ETAPI 已使用已授权令牌完成真实根节点读取、验证节点创建、revision、管理标记更新、人工区域保护、标记缺失冲突和离线恢复，重复连接器任务没有生成重复节点，公网 `/etapi/` 仍为 `404`
- 线上 Core 已更新到当前提交，旧版本缺少 `commit_id` 的 ACK 问题已修复，当前 ACK 包含稳定提交标识，乱序上传、精确重传和租约冲突已在 VPS 实例复核
- RTX 4080 GPU Agent 已通过私有 Worker Gateway 上线，线上读回为 `faster-whisper:small@cuda`、`ollama:qwen2.5:7b-instruct@cuda`，GPU 遥测包含显存、功耗、温度和利用率
- 当前提交的 90 分钟网络音频正式门已完成，使用 HTTPS 受控课程音频、真实 WSS、VPS SQLite、ReadWeave 连接器和 RTX 4080；5374 个音频块全部确认，最终状态为 `session.completed`
- 原隔离 6 小时长测曾发生一次 Windows Core 访问冲突并自动保留 WER 证据，Core 已在原数据目录恢复运行；最终状态为 `failed`，该轮不能标记为无故障通过
- 6 小时目标会话复核结果为 21324 个音频块、0 重复、0 序号缺口、5864 个稳定片段、1600 个译文、83 个讲解卡、0 GPU OOM；1 个翻译任务最终以 `provider_response_invalid` 失败，Core 重启后的长尾延迟使发布门失败
- 24 小时低负载监测进程已经停止，未找到正式结果 JSON；停止原因和可恢复的部分采样目前无法证明，下一轮必须从新结果路径重新执行
- Android 一次性公网配对入口已修复并通过真实 `200 application/json` 读回；当前设备已完成前台和锁屏声学录音，服务器确认 815 个音频块、33 个连贯段落、33 个译文、3 个教学块和 1 份总结，服务已安全停止并进入 `completed`；MacBook 外部 Chrome 当前仍没有可附着标签，真实设备发布门尚未关闭

2026-08-31 的 1 分钟运行证明链路、30 分钟网络音频预检、当前提交的 90 分钟正式长测、双设备浏览器验收和 Android 前台／锁屏收音已通过；6 小时隔离长测已因 Core 访问冲突和 1 个失败任务判定未通过，24 小时低负载监测已中断且无正式结果，MacBook 登录验收仍未按本版协议完成

之前一次 90 分钟运行的最终总结约 291 秒，超过 30 秒同步预算和 120 秒异步预算，因此该轮不能标记为完整通过

生产入口经 Authentik 登录后直接进入稳定的 `/app` 工作区路由，不再显示一次性营销入口页

VPS 负责身份、项目、音频持久化、ACK、任务队列和事件，本机 GPU Agent 通过私有通道主动领取任务，GPU 离线时音频继续保存且不生成占位结果

## 2 已完成能力

### 2.1 持续工作区

- 左侧树组织文件夹、课程项目、课程会话和五类固定笔记
- 项目和会话路由可刷新、可收藏，并按设备恢复最后访问位置
- 中间区域使用连续课程段落，可切换双语、仅原文或仅译文
- 总结、背景补充、术语、疑似听写、复习问题和课件证据分别标识
- 正文默认只挂载最新 160 项，完整时间线仍保存在事件库和 ReadWeave，长课不会无限扩大 DOM
- 窄屏使用抽屉式树，所有操作仍在同一页面完成

### 2.2 身份、多端与录音租约

- Authentik 稳定 UID 是项目所有者主键，生产缺少可信代理身份时返回 `401`
- 同一用户的不同设备看到相同项目树、会话、字幕、译文、讲解和同步状态
- 不同用户读取彼此项目时返回 `404`
- 同一项目只有一个有效录音租约，第二台设备申请时返回 `409`
- 租约 10 秒续期、45 秒有效，接管后 generation 递增，旧租约上传被拒绝
- 音频密钥放在 WebSocket 子协议中，不进入 URL 和普通日志
- Android 配对交换仅允许短期单次配对码，不要求新设备先建立 Authentik 会话；除该入口外生产 API 仍要求受信代理身份
- GPU Agent 读取任务对象时必须证明当前租约 Worker 身份，错误 Worker 不能读取其他任务输入

### 2.3 录音与恢复

- 浏览器使用 AudioWorklet 采集，未确认帧进入 IndexedDB
- ACK 只在音频对象和 SQLite 事务完成后返回
- 持久装配器按音频块和游标重建窗口，Core 重启不会丢失未满窗口尾音
- 自适应窗口使用 1.5 秒最小语音、450 毫秒静音和 5 秒硬上限
- 麦克风测试在本机完成音量、噪声底和削波判断，不申请租约、不上传音频
- Android 支持短期配对码、前台服务、先落盘后发送和 ACK 后清理

### 2.4 分层模型

| 任务 | 当前默认 | 原因 |
|---|---|---|
| 实时 ASR | `faster-whisper:small@cuda` | 当前 90 分钟正式长测端到端 p95 为 1306 毫秒 |
| 实时翻译 | `qwen2.5:7b-instruct@cuda` | 当前 90 分钟正式长测端到端 p95 为 2508 毫秒 |
| 滚动讲解 | `qwen2.5:7b-instruct@cuda` | 当前 90 分钟正式长测端到端 p95 为 5321 毫秒 |
| 最终总结 | `qwen2.5:14b-instruct@cuda` | 当前 90 分钟正式长测只生成 1 次，耗时 20398 毫秒 |
| 图片解析 | `qwen3-vl:8b-instruct@cuda` | 只在实时队列空闲时运行 |

旧链路每个声学片段都创建翻译任务，并按每五个片段触发讲解，7B 因任务过密而阻塞翻译；V17 把 82 个声学片段整理为 23 个连贯段落，只生成 23 项翻译和 3 个教学块，7B 翻译与讲解 p95 分别为 1.126 秒和 4.214 秒

最终总结从整场时间轴均匀抽取连贯段落、滚动讲解和材料页，不再只读取课程开头；当前 90 分钟正式长测的 14B 总结为 20398 毫秒并处于同步预算内，早期 90 分钟运行约 291 秒并超过异步预算

### 2.5 ReadWeave

- AIALRA 追加式事件库是事实源，ReadWeave 是可重建文本投影
- 用户文件夹、项目、会话和系统笔记保存稳定远端 ID
- 自动同步只修改 AIALRA 管理标记内的内容，不覆盖 `99 我的笔记`
- ReadWeave 离线时连接器任务持久排队，恢复后按幂等键补写
- 每个系统节点可在当前标签跳转到对应 ReadWeave 笔记
- 浏览器永远不会收到 ETAPI Token

### 2.6 界面与运行状态

- 主视觉为黑、白和中性灰，红色用于录音和错误，绿色用于在线和完成，黄色用于排队和恢复
- 输入正文与占位文字在亮色和暗色主题均设置明确颜色
- GPU 面板显示利用率、显存、功耗、温度、驻留模型、当前任务和队列深度
- GPU 遥测不返回主机名、进程、设备 UUID、序列号或私有网络信息
- 14B 总结和图片解析使用独占调度，避免与 ASR 同时加载造成显存竞争
- 14B 最终总结 Worker 超时固定为 120 秒，超过预算会进入可见失败并保留原任务重试
- 模型任务表为旧数据库兼容保留 `queued`、`leased`、`completed` 和 `failed` 状态，细分运行态通过追加事件记录，取消接口尚未开放
- ReadWeave 跳转和工作区操作不会自动打开新页面或新标签

## 3 正式验证结果

### 3.1 当前可复核短测

- 项目同步与录音租约：同用户视图一致、跨用户返回 `404`、第二录音设备返回 `409`、租约接管后 generation 递增、旧租约被拒绝
- 音频顺序与重传：乱序上传恢复为连续窗口，精确重传返回稳定 commit ID，不产生重复段落
- Core 重启恢复：未满窗口的尾部音频在重启后恢复，未产生重复字幕、段落或总结
- 安全边界：生产代理标记、Origin 白名单、跨项目 IDOR、旧版启动接口和旧租约 WebSocket 均已通过自动检查
- 最新运行证明 1 分钟网络音频：60 个音频块全部 ACK，正常网络 ACK p95 为 7 毫秒，断网影响 ACK p95 为 5014 毫秒，ASR p95 为 1420 毫秒，翻译 p95 为 1624 毫秒，14B 总结为 16798 毫秒，重复段落、GPU OOM 和假结果为 0
- 30 分钟网络音频预检：1782 个音频块全部 ACK，三次断网恢复，490 个声学片段整理为 134 个连贯段落和 134 个译文，22 个教学块，ASR p95 为 474 毫秒，翻译 p95 为 1492 毫秒，讲解 p95 为 3046 毫秒，14B 总结为 18737 毫秒，重复段落、GPU OOM 和假结果为 0
- 浏览器双设备真实链路：双设备观察、断网缓存、刷新恢复和全部 ACK 通过；Chrome 实测产生 9 个稳定段落和 3 个稳定译文
- 当前提交的 90 分钟正式网络音频：5374／5374 个音频块确认，1478 个声学片段整理为 403 个连贯段落和 403 个译文，63 个教学块，1 份 14B 总结，三次断网恢复，正常 ACK p95 为 152 毫秒，全量 ACK p95 为 203 毫秒，ASR、翻译、讲解和总结端到端 p95 分别为 1306、2508、5321 和 20398 毫秒，重复、GPU OOM、失败任务和假结果均为 0
- Android 真机前台与锁屏收音：815 个音频块确认，103 个声学片段整理为 33 个连贯段落和 33 个译文，3 个教学块和 1 份总结，327 个模型任务全部完成，服务安全停止后会话进入 `completed`

### 3.2 历史 90 分钟基础设施运行（不作为当前发布门）

受控公开合成课程音频通过 HTTPS、真实 WSS、VPS SQLite、私有 Worker 通道和本机 RTX 4080 运行 90 分钟，这一结果证明旧版基础设施完整性，不替代 V17 段落质量长测

| 指标 | 历史观测 | 当前解释 |
|---|---:|---:|
| 音频块 | 5328 个全部 ACK | 仅证明旧运行的持久接收完整性 |
| 音频序号缺口 | 0 | 仅作为历史恢复证据 |
| 稳定字幕 | 1466 | 旧运行的结果数量，不代表当前质量门 |
| 稳定译文 | 1466 | 旧运行的结果数量，不代表当前质量门 |
| 讲解卡 | 278 | 旧运行的结果数量，不代表当前节奏门 |
| 网络中断 | 5 秒、15 秒、60 秒 | 旧运行自动恢复，当前版本需重新复核 |
| ASR p95 | 1.284 秒 | 历史单阶段观测，不覆盖当前发布判定 |
| 翻译 p95 | 7.886 秒 | 历史单阶段观测，不覆盖当前发布判定 |
| 讲解 p95 | 10.678 秒 | 历史单阶段观测，不覆盖当前发布判定 |
| GPU OOM | 0 | 历史观测，当前长测仍待完成 |
| 假结果 | 0 | 历史观测，当前数据库门仍需复核 |

该历史运行的完整性数据仍可用于定位恢复行为，但最终 14B 总结约 291 秒，超过同步和异步预算，故不能作为当前版本的完整通过证据

### 3.3 工作区与恢复门禁

- 同用户双设备项目视图一致，不同用户隔离通过
- 第二录音设备冲突、45 秒后接管、旧租约拒绝通过
- SSE 使用游标恢复，重连后无遗漏和重复
- Core 重启后 756 个音频块恢复为 208 条字幕和 208 条译文，所有 435 个模型任务只完成一次
- ReadWeave 离线时任务排队，恢复后第 4 次尝试成功，7 个映射节点均为已同步
- Qwen3-VL 8B 真实图片解析冷启动 48.743 秒，热运行 12.906 秒

### 3.4 V17 连贯段落 30 分钟门禁（历史记录）

该表保留用于追踪上一版段落装配和模型节流修复；当前版本的总结预算与运行证明已经收紧，不能用这组历史数字替代 2026-08-31 之后的长测

| 指标 | 结果 | 门禁 |
|---|---:|---:|
| 音频块 | 1777 个全部 ACK | 丢失 0 |
| 完整客户端断网 | 5 秒、15 秒、30 秒 | 全部恢复 |
| 内部声学片段 | 489 | 只作追加式证据 |
| 连贯段落与译文 | 134／134 | 数量一致，重复 0 |
| 教学块 | 22 | 大段落且队列排空后触发 |
| 最终总结 | 1 | 14B CUDA |
| ASR p95 | 0.914 秒 | 不超过 3 秒 |
| 翻译 p95 | 1.078 秒 | 不超过 8 秒 |
| 讲解 p95 | 4.194 秒 | 不超过 20 秒 |
| 总结 | 18.356 秒 | 不超过 30 秒 |
| GPU OOM | 0 | 必须为 0 |

### 3.5 自动检查

- Rust 格式、`clippy -D warnings` 和 44 项测试通过
- Web ESLint、TypeScript、21 项测试和生产构建通过
- Python Ruff、mypy 和 25 项测试通过
- 中英文 README 严格审核为 0 错误、0 提醒，亮暗主题在 1280 与 390 像素视口均无横向溢出且图片完整加载
- Android 单元测试和 Debug APK 构建通过
- 前端生产依赖审计没有已知漏洞
- RustSec 使用 1226 条公告扫描 225 个依赖，没有漏洞或警告

## 4 当前限制

- V17 连贯段落链路和最新 90 分钟网络音频已通过受控合成课程门禁，但不能替代真实 MacBook 课堂内容的人工可读性盲评
- 6 小时稳定性已经执行但因 Core 访问冲突和最终失败任务未通过；24 小时低负载 soak 已中断且无正式结果，当前发布门仍保持 `PARTIAL / NOT RELEASE SIGN-OFF`
- 同轮 35 分钟资源监控显示 Worker 增长 2.55 MiB、Core 匿名内存下降 0.1 MiB，总 GPU 显存采样受另一个用户 GPU 程序污染，不用于模型泄漏判定
- Android 配对、前台录音、锁屏上传、ACK 清理和页面安全停止已有本轮证据；通知栏停止、来电、蓝牙、Wi-Fi 切换和耗电矩阵仍未完成
- iOS 只承诺 Safari 前台录音，不承诺锁屏后台录音
- 云端增强默认关闭，当前没有单独的 AIALRA 云端模型凭据，因此不会假装完成 Terra 或 Sol 调用
- 线上 ReadWeave ETAPI 已完成真实读写与离线恢复验证，下方旧部署段落中的“无可用实例”仅是历史记录，不代表当前环境
- 摄像头、自动截图、嵌入检索、复杂公式和图表理解尚未进入生产闭环
- 第一版只支持同一 Authentik 用户的多设备访问，不包含多人协作权限
- 原始音频保留期、删除、导出和备份界面尚未完成
- DingTalk 公开资料仍未证明第三方连续 PCM 或增量逐字稿能力

## 5 历史部署记录与当前边界

- 线上已部署当前提交到新的不可变发布目录，并保留部署前环境、SQLite、对象目录和 ReadWeave 数据备份
- 线上 Core 已切换到当前提交并完成健康、身份、Origin、ACK 和 ReadWeave 读回；6 小时 Core 访问冲突与设备侧待完成项仍不允许关闭发布门
- 当前版本的数据库、模型和配置应在正式上线前重新读回，不能沿用旧部署记录作为发布证明
- 本地真实浏览器验收确认稳定 `/app` 路由、亮暗主题持久化、内置工作区弹窗、三种语言视图、完整教学块、麦克风测试、GPU 指标和 760 像素窄屏无溢出，控制台错误与警告为 0
- v17 五分钟生产预检完成 295／295 个 ACK、82 个内部片段、23 个连贯段落、23 个译文、3 个教学块和 1 份 14B 总结，两次断网后重复与 OOM 为 0
- v18 五分钟最终回归完成 298／298 个 ACK，23 个段落全部具有整理后原文和译文，7B 翻译 p95 为 1.256 秒，14B 总结为 16.019 秒
- Windows 登录启动项已重新安装，当前会话与后续登录都锁定 7B 翻译、7B 讲解和 14B 总结；本机健康接口、Ollama 驻留模型和线上 Worker 心跳三方读回一致
- 生产深色窄屏在 390 像素视口无横向溢出，浏览器控制台错误为 0
- Core 录音中途重启后，2 个已确认块恢复为 1 个连贯段落、1 个译文和 1 份 14B 总结，尾音与最终事件没有重复
- 修复已完成课程在 SSE 历史回放期间短暂倒退为录音中的问题，状态单调回归测试与线上复核均通过
- 13 条早期合成验证留下的 fake／fallback 结果已在完整 SQLite 在线备份后定向移除，生产事件和模型任务复核均为 0，ReadWeave 对应投影已重新校对
- Authentik、公开路径保护、内部 Worker 路径隔离和 ReadWeave ETAPI 公网隔离已通过读回
- GitHub 最终候选仍需在本轮全部测试和文档完成后重新执行工作树、历史、图片和元数据安全门禁
- 仓库保持 Public 且没有许可证，公开可见不代表授予复制、修改、分发或商用许可

下一阶段只收尾 6 小时稳定性、24 小时低负载 soak、真实 MacBook 课堂内容盲评和 Android 网络／系统状态真机矩阵；材料检索、摄像头取证、DingTalk 扩展和云端高精度复核不属于当前收尾范围

## 2026-09-01 feedback-01 候选（待远端部署）

本批针对用户实际反馈收口后台错误路径、录音认证和工作区 UI；在合并并完成远端快速冒烟前，线上仍保持已部署的 v25，不把本地候选写成已上线。

- 浏览器原有录音控制路径恢复经过 Authentik；Android 改用独立的设备控制别名，浏览器不再展示备用配对入口，音频与事件仍共享同一项目会话。
- durable 音频 ACK 不再被项目通知或 ReadWeave 辅助投递失败否定；停止、重复停止、租约冲突和 GPU 领取后异常均进入可重试且可见的安全状态。
- Core、GPU Agent 和模型 Worker 的异常输出改为安全错误码／阶段／长度／摘要；ReadWeave 创建后映射恢复、重复校对去重和所有权校验已补齐。
- 工作区增加固定回收站、右键操作、独立展开按钮、拖拽排序／移动、分类系统笔记、完整主题变量、只读设置与运行状态，以及 ReadWeave 连接目标、同步范围、最近同步和打开前确认。
- 本批本地快速检查实际通过：Rust 格式、workspace 测试、Clippy、Python 30 项测试／Ruff／mypy、Web 21 项测试／类型检查／ESLint／生产构建、Node 语法和 `git diff --check`；未启动本地 Docker、Docker Desktop 或 WSL。
- Android Gradle 当前运行被本机 Java loopback 建连错误阻断，未据此判定业务代码通过；远端 Bash 语法、GitHub 合并、VPS 部署和新版本生产冒烟仍待本批后续步骤。
- 文档和测试记录只保留脱敏指标，不包含令牌、真实会话 ID、私有路径、录音、模型原文或原始日志。
