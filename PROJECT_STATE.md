# AIALRA-LIVE-TRANSLATE 项目状态

## 1 状态摘要

本次实施日志记录的状态日期：2026-08-26

当前版本：`0.1.0` 浏览器与本地纵向切片

当前结论：重新定位后继续

Android 真机已经证明 USB 本地收音、确认、持久化和应用内停止链路可用；浏览器入口已通过受 Authentik 保护的 VPS、Cloudflare 代理 DNS 和 HTTPS 上线。VPS 合成验收证明音频 ACK、字幕、译文、材料和讲解链路完整，但 CPU 译文与讲解延迟不满足长课实时目标。

## 2 门禁状态

验证报告记录以下工程门禁：

- 研究包完整性通过：ZIP 和包内 `SHA-256` 全部匹配
- 本机环境审计通过：硬件、工具链、Ollama 和 BabbleDeck 已核验
- 事件存储状态机通过：Rust 测试、SQLite WAL、内容寻址对象和会话转换已验证
- 浏览器确定性体验通过：Playwright 完成创建、播放、材料、讲解和停止流程
- 本地真实模型闭环通过：端到端日志记录 3 个语音窗口全部产生字幕和译文
- Android 构建通过：配置文件设定 Android 36，调试 APK 与字节序测试通过
- Android 真机收音部分通过：realme RMX5060、Android 15 的两轮用户确认测试完成，服务端确认后的手机缓存为零
- Android 应用内停止通过：无声自检接收 3 个音频块后，核心会话完成、服务登记清空且手机缓存为零
- DingTalk 公开能力适配部分通过：控制和补偿接口已接入，真机与企业授权待验证
- 90 分钟真实课程待执行：时长来自下一轮验证计划，需要课程许可、A1 真机和目标用户
- VPS 受保护展示通过：Cloudflare 代理 DNS、HTTPS、共享 Authentik、Nginx、三项容器健康检查和合成端到端验收均已取得线上回执

## 3 已完成范围

- Rust 模块化核心、追加式事件、SQLite WAL 和内容寻址文件目录
- 浏览器 PCM 采集、WebSocket 序号、ACK、重连和服务端幂等落盘
- Android 前台录音服务、原子缓存、断线重发和 ACK 后清理
- Android 前台状态恢复、通知栏停止命令和应用内停止命令
- 配置文件设定连续语音最长保留 8 秒；ASR 在完成识别后立即释放优先许可，短暂停顿触发字幕结算
- `faster-whisper small` CUDA 运行、Ollama 翻译与结构化讲解
- PPTX、PDF、DOCX、图片、文本和 Markdown 解析
- 手动讲解与配置文件设定的每 5 条稳定字幕一次的周期讲解
- DingTalk A1 录音控制、DVI 能力探针和小程序前台录音帧探针
- 课程许可、本地默认、明显录音状态和云端出口关闭
- 电脑与手机浏览器共用 HTTPS/WSS 收音入口，可选择系统默认或指定麦克风
- VPS CPU 部署包、Cloudflare 代理 DNS 自动化、共享 Authentik 网关和 Nginx WebSocket 配置
- 受保护的公网展示入口、自动续期证书、CPU `tiny/int8` 字幕档和 1.5B 本地译文/讲解档

## 4 当前限制

- A1 实时 PCM 与增量逐字稿没有公开或真机证据
- Android APK 已在 realme RMX5060、Android 15 完成 USB 收音与 ACK 清理验证，型号和系统版本来自本轮真机设备记录；锁屏、耗电、来电、蓝牙和 Wi-Fi 切换仍待验证
- 固定窗口会切断受控英语短句并产生词错，4 秒数值来自本轮真机事件记录

- 静音分段代码已通过自动测试，真机识别质量复测待执行
- 图片目前保留原图并进入时间线；OCR 和视觉语言模型尚未纳入默认路径
- 翻译与周期讲解已经从 ASR 优先许可中移出；长课程仍需测量 GPU 争用、队列增长和取消行为
- 桌面端当前由 Rust 服务网页提供；Tauri 安装壳尚未完成
- 没有连接 DeepSeek；云端 Provider 保持关闭
- 浏览器未确认块仍只在内存中，刷新页面不能像 Android 一样从持久缓存恢复
- VPS 没有 GPU；CPU 展示配置不能替代长课延迟与质量门禁
- VPS 最终合成验收中，12.410 秒音频的最后字幕在 12.8 秒出现，但译文在 39.3 至 102.9 秒出现，说明字幕接近实时、译文尚不实时

## 5 下一门禁

- 第一步，在已上线的 Authentik 展示入口完成一次真实浏览器麦克风许可与 WSS 收音验收；浏览器权限必须由登录用户亲自授予

- 第二步，复测 Android 通知栏停止、静音分段与短句识别，再运行 A1 与 Android 双录音 90 分钟

- 第三步，记录音频完整率、稳定字幕 p50 与 p95 延迟、关键术语召回、显存峰值、耗电和 A1 会后对账差异

- 第四步，邀请至少一名目标学生和课程教师复核许可流程、讲解密度与实际理解收益

- 第五步，优先实现 VPS 持久任务队列与 RTX 4080 私有 GPU Worker 连接器，把实时译文从 VPS CPU 移出，再评估 Tauri、摄像头和云端增强
